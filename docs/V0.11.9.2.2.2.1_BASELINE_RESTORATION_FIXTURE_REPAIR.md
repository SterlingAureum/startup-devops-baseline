# v0.11.9.2.2.2.1 baseline restoration fixture repair

The v0.11.9.2.2.2 runtime guard exposed an inherited offline-test gap. The fake
Kubernetes client used by the v0.11.3.5 restoration regression returned no
Rollout JSON, so the new runtime assertion correctly observed empty phase,
version and ReplicaSet identities. The real cluster remained Degraded on the
already-known aborted revision 67; this was not a new runtime failure.

The fake client now returns a minimal Healthy Rollout with a non-empty
application version and identical current/stable hashes. The historical output
assertion expects `restored and runtime-qualified`, and the focused v0.11.9.2.2.2
validator invokes the inherited restoration regression directly.

This increment is an offline validation repair. Applying or validating it does
not contact Kubernetes, retry or promote a Rollout, generate traffic, or mutate
AWS resources.
