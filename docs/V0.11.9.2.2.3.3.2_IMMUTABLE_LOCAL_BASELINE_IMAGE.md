# v0.11.9.2.2.3.3.2 immutable local baseline image

## Published identity

Image-publish workflow run `34070524953` completed successfully for source
commit `cf0a6bcbc466b61f2018a0a92c961d7c03f128e8`. Quality gates, build-and-push,
vulnerability scanning, SBOM generation, and provenance/SBOM attestations
completed before GHCR publication. Automatic promotion was disabled.

The local default baseline now declares:

```text
repository: ghcr.io/sterlingaureum/startup-devops-baseline/demo-api
tag: sha-cf0a6bc
digest: sha256:cdffd3d71763540976570da1f201661d24c641ec459be812b20f1517f3fd2623
application version: sha-cf0a6bc
release ID: demo-api-cf0a6bcbc466-cdffd3d71763
```

The digest, not the mutable tag, is the runtime image selector. Tag, source
commit, application version and build run remain correlated evidence.

## Restoration preflight

`restore-local-feature-baseline.sh` retains the explicit revision 69 rejected
image guard. It additionally derives and validates the declarative release ID
before invoking the shared restoration script. An invalid or incomplete tag,
source commit, digest or application version stops before any Kubernetes
operation.

Applying and validating this increment does not restore the baseline, start an
AnalysisRun, generate traffic, retry/promote revision 69, or call AWS.

## Next operator boundary

After this increment passes local validation and is committed and pushed, arm
`first-analysis` before executing exactly one reviewed feature-baseline
restoration. That operation must create a fresh Rollout revision with the new
release ID. Use post-observer AnalysisRun selection; do not count or reuse the
failed revision 69 runs. At the 50% pause, arm `second-analysis`, wait until its
bounded traffic begins, then promote exactly once. Final acceptance requires a
Healthy Rollout with stable/current hash equality and two successful new
AnalysisRuns for the new release ID.
