# v0.11.9.2.2.3.3.3 baseline restoration closure

## Accepted runtime evidence

The digest-pinned local baseline declared by v0.11.9.2.2.3.3.2 completed a
fresh rollout as revision 70. The accepted identity is:

```text
application version: sha-cf0a6bc
release ID: demo-api-cf0a6bcbc466-cdffd3d71763
source commit: cf0a6bcbc466b61f2018a0a92c961d7c03f128e8
image digest: sha256:cdffd3d71763540976570da1f201661d24c641ec459be812b20f1517f3fd2623
stable ReplicaSet: 7557fdfb9b
```

Both release-scoped AnalysisRuns completed successfully:

```text
demo-api-7557fdfb9b-70-2  Successful
demo-api-7557fdfb9b-70-5  Successful
```

The final Rollout was Healthy at step 7/7 with `abort=false`, three desired and
three ready replicas, and identical stable/current hashes. Revision 69 and its
failed AnalysisRun remain retained incompatibility evidence; neither retry nor
promotion was performed against that revision.

## GitOps and idempotency closure

Root application `startup-devops-root` and child application `demo-api` both
converged to reviewed revision
`ab03a439f44c9766b951668dd344b6717e327a03` as Synced and Healthy. Both Argo CD
diff commands returned empty results.

Re-running the feature-baseline restoration for the same reviewed revision
returned `Pre-merge feature baseline restored and runtime-qualified.` The
Rollout remained revision 70 with the same stable ReplicaSet and no revision 71,
demonstrating that the restored declaration is idempotent.

## Portable tag parsing

The successful idempotent run exposed one benign mawk warning from the quote
removal expression used while reading the image tag. The expression now uses a
literal double quote in the awk regular expression. The offline validator runs
the same parser, requires `sha-cf0a6bc`, and rejects any stderr output.

Applying or validating this increment performs no Kubernetes, Argo CD or AWS
operation and does not generate traffic, promote or retry a Rollout.

## Final feature-revision alignment

After this increment is validated, committed and pushed, update Root and
same-repository child target revisions to that new commit with the reviewed
feature-baseline restoration command. The Helm runtime declaration is unchanged,
so this final GitOps alignment must keep Rollout revision 70, application
version `sha-cf0a6bc`, stable ReplicaSet `7557fdfb9b`, and Healthy status. A new
Rollout revision, AnalysisRun, pause, degraded phase, or non-empty Argo CD diff
is a stop condition; do not retry or promote it.
