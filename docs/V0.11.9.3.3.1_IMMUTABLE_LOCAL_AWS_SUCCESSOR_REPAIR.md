# v0.11.9.3.3.1 immutable local AWS successor repair

## Purpose and baseline

This checkpoint repairs one inherited validation conflict discovered by the
reviewed existing-image promotion PR. Applying or validating it does not change
a release file or workflow, rerun PR 71, rebuild an image, access AWS or
Kubernetes, or change local runtime state.

The exact implementation baseline is:

```text
03e39f3416a8f5e368d0cda497487376ec575d52
```

## Observed failure

Existing-image workflow run `34180004676` succeeded from the exact main
baseline and created PR 71 for `sha-cf0a6bc`. Release currentness passed, but
the inherited quality gates stopped in:

```text
scripts/validate-v0.11.9.2.2.3.3.2-immutable-local-baseline-image.sh
```

That historical checkpoint correctly proved that its original change affected
only local default values. Its validator expressed the historical state as a
permanent rule: the local candidate digest and source commit could not appear
in any AWS release file. A later promotion of the same accepted artifact to
aws-dev therefore failed even though v0.11.9.3.3 selected that exact successor.

## Repair semantics

The original local baseline remains exact and unchanged. The historical
validator continues to reject the selected candidate from aws-test and
aws-prod. Only the aws-dev check becomes successor-aware, and it delegates to
the single allowlist introduced by v0.11.9.3.3.

Allowed aws-dev states are limited to:

1. the original release file fingerprint; or
2. the complete `sha-cf0a6bc` identity, including repository, digest, source
   repository, source commit and workflow run ID.

The distinct `sha-6013688` digest, partial metadata, and every unknown identity
remain rejected. The repair does not create a second source of promotion truth.

## Validation

Run:

```bash
bash scripts/validate-v0.11.9.3.3.1-immutable-local-aws-successor-repair.sh
bash scripts/validate-ci-quality-gates.sh
```

The focused validator exercises the historical pre-promotion file, the exact
selected successor and the rejected incidental image against the repaired
historical entrypoint. It also confirms no release file or workflow changed.

## Promotion hold

Keep PR 71 open and unchanged while this repair is reviewed. Do not rerun,
update or merge it before the repair reaches protected main and the new main
checks pass.

After the repair is merged, first inspect PR 71 for an automatic fresh check.
If its failed check is still the old run, update its branch from current main
once, then wait for the new pull-request checks. The PR must still differ from
main by exactly `apps/demo-api/helm/values/releases/aws-dev.yaml` before it can
be reviewed for merge.
