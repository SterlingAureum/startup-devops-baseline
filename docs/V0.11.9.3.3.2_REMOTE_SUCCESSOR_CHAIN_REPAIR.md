# v0.11.9.3.3.2 remote successor chain repair

## Purpose and baseline

This checkpoint repairs the remaining permanent pre-promotion assumptions in
the v0.11 validation chain. Applying or validating it does not change a release
file or workflow, mutate PR 71, dispatch GitHub Actions, rebuild an image,
access AWS or Kubernetes, or change local runtime state.

The exact implementation baseline is:

```text
6f259e229a8f653e1a567fe809ed58926d31bbb4
```

## Observed inherited failure

After v0.11.9.3.3.1 reached main, PR 71 advanced beyond the previously repaired
identity validator and stopped in the v0.11.9.3.0 design validator. Embedded
Python line 87 required the selected candidate digest to remain absent from all
three AWS release files.

That assertion accurately described the offline design checkpoint but cannot
remain a permanent repository rule after the reviewed aws-dev promotion begins.
The same audit also found that the v0.11.9.3.3.1 regression validator still
compared the current aws-dev file to its pre-promotion fingerprint. Without a
second repair, it would become the next deterministic failure.

## Unified successor semantics

Both validators now delegate current aws-dev state to the existing
`check-v0.11.9.3.2-release-successor.py` allowlist. No new source of candidate
truth is introduced.

Exactly two aws-dev states remain valid:

1. the pre-promotion fingerprint; or
2. the complete selected `sha-cf0a6bc` identity.

The historical design facts remain unchanged. aws-test and aws-prod still must
not contain the candidate. The distinct `sha-6013688` image and every partial
or unknown identity remain rejected.

## Validation

Run:

```bash
bash scripts/validate-v0.11.9.3.3.2-remote-successor-chain-repair.sh
bash scripts/validate-ci-quality-gates.sh
```

The focused validator executes both repaired historical entrypoints against the
current file, the selected successor and the rejected incidental image. It also
scans the v0.11 validation scripts for the two known permanent-absence patterns.

## Promotion hold

Keep PR 71 open and unmerged until this repair reaches protected main and the
new main checks succeed. Do not rerun the historical failed workflow run.

After the repair merges, inspect PR 71. If GitHub has not created fresh checks
against the new main, run `gh pr update-branch 71` once and wait for the new
checks. Before merge, the PR diff must still contain exactly
`apps/demo-api/helm/values/releases/aws-dev.yaml` and its complete identity must
equal the selected candidate.
