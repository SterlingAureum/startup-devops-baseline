# v0.11.9.3.3 reviewed main integration

## Purpose and baseline

This checkpoint records the reviewed v0.11 integration at protected main and
repairs the historical release-file guard exposed by the automatic main image
publisher. It does not dispatch a workflow, merge or close a pull request,
access AWS or Kubernetes, reconcile Argo CD, or change any release values.

The exact implementation baseline is the PR 68 merge commit:

```text
6013688a384c4e39a5b2b79d1b95bb29d00725de
```

PR 68 integrated feature head `7192f28f947e9f1942cfc67d093dc00afa7a0755`.
All three pull-request checks and the four observed main-push workflows
completed successfully. Every active AWS same-repository Application continues
to follow `main`.

## Incidental main image

The merge changed paths watched by `demo-api-image-publish.yaml`. Main-push run
`34124828745` therefore built and attested a separate image:

```text
tag: sha-6013688
digest: sha256:445f8e432cd0848a615f99a123a8305262255bd4a6082675a58744742660b02d
source commit: 6013688a384c4e39a5b2b79d1b95bb29d00725de
release ID: demo-api-6013688a384c-445f8e432cd0
```

That run also created PR 69. The image passed its build gates but did not run
the reviewed local revision 70 qualification. It is not the selected rehearsal
candidate. PR 69 must be closed without merge and its image identity must not be
written to aws-dev.

The failure observed on PR 69 was a useful fail-closed result: the historical
v0.11.9.3.2 validator fingerprinted the pre-promotion release file. However,
that absolute fingerprint would also reject the intended existing-image
promotion. This checkpoint makes the historical guard successor-aware while
remaining allowlist-based.

## Single selected successor

The only allowed aws-dev successor remains the locally qualified image:

```text
tag: sha-cf0a6bc
digest: sha256:cdffd3d71763540976570da1f201661d24c641ec459be812b20f1517f3fd2623
source commit: cf0a6bcbc466b61f2018a0a92c961d7c03f128e8
workflow run: 34070524953
release ID: demo-api-cf0a6bcbc466-cdffd3d71763
```

The release guard accepts exactly two aws-dev states:

1. the original SHA-256-fingerprinted release file; or
2. a complete release identity exactly equal to the selected candidate above.

It rejects `sha-6013688`, partial identity changes, unknown digests and every
other successor. The aws-test and aws-prod release files remain fixed.

## Validation

Run the focused checkpoint and complete quality gates:

```bash
bash scripts/validate-v0.11.9.3.3-reviewed-main-integration.sh
bash scripts/validate-ci-quality-gates.sh
```

The focused validator exercises the original release state, the one allowed
successor and the rejected incidental image without network, AWS or cluster
access.

## Deferred execution

Apply this increment only on a clean branch created from the exact protected
main baseline above. Review and merge it through a separate pull request.

After that pull request is merged:

1. close PR 69 without merging it;
2. confirm no other demo-api release PR is open;
3. confirm aws-dev still has the historical file fingerprint;
4. dispatch `demo-api-promote-existing-image.yaml` from protected main with
   source run `34070524953` and source commit
   `cf0a6bcbc466b61f2018a0a92c961d7c03f128e8`;
5. review the resulting release-only PR before any merge.

Do not run the two promotion paths concurrently. No AWS environment or runtime
qualification is authorized by this checkpoint.
