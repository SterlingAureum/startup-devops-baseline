# v0.11.9.3.4 existing-image aws-dev promotion execution

## Purpose and baseline

This checkpoint records the completed Git handoff of the locally qualified
build-once image into the aws-dev release declaration. It does not create an
AWS environment, reconcile Argo CD, collect runtime evidence, promote to
aws-test, or authorize a live operation.

The exact implementation baseline is protected-main commit:

```text
071e32914a304f6e3ca006f0f60f54b21b5d810d
```

## Reviewed handoff

Existing-image workflow run `34180004676` completed successfully from main
commit `03e39f3416a8f5e368d0cda497487376ec575d52`. It reused source image workflow
run `34070524953`; no image was rebuilt.

The workflow created PR 71 on `release/demo-api-sha-cf0a6bc`. After the
successor-chain repairs reached main, the branch was updated and its fresh
checks passed. The final PR changed exactly:

```text
apps/demo-api/helm/values/releases/aws-dev.yaml
```

The final PR head `2353977e3efa1f202a0bfe3cd1c2b9f6a4b505d2` was merged as
`071e32914a304f6e3ca006f0f60f54b21b5d810d` at
`2026-09-08T07:13:30Z`.

## Immutable aws-dev identity

The protected-main aws-dev declaration now contains:

```text
repository: ghcr.io/sterlingaureum/startup-devops-baseline/demo-api
tag/application version: sha-cf0a6bc
digest: sha256:cdffd3d71763540976570da1f201661d24c641ec459be812b20f1517f3fd2623
source commit: cf0a6bcbc466b61f2018a0a92c961d7c03f128e8
source workflow run: 34070524953
release ID: demo-api-cf0a6bcbc466-cdffd3d71763
release file SHA-256: 5238e8bcdfb23afb882eaabda6b3f732f5a2f461cc38bd9f09d26c8fff7a5d46
```

aws-test and aws-prod retain their prior identical fingerprints. The selected
candidate is absent from both later environments.

## Post-merge checks and recursion boundary

For exact main commit `071e32914a304f6e3ca006f0f60f54b21b5d810d`, validate run
`34198278046` and release-orchestrator run `34198278111` both completed
successfully. No image-publish run was observed for that commit. This matches
the repository path boundary: a release-values-only merge can wake the release
orchestrator but is excluded from image-publish paths.

## Runtime boundary

The merge changes Git desired state only. It is not evidence that aws-dev
exists, that Argo CD reconciled the commit, that the Deployment uses the exact
digest, or that observability qualification passed. Do not prepare aws-test
promotion from this record.

The next checkpoint must perform a separate aws-dev live-rehearsal preflight.
It must select exact main `071e32914a304f6e3ca006f0f60f54b21b5d810d`, confirm no
other EKS rehearsal environment is active, validate operator-private inputs and
cost boundaries, and retain explicit approval before any AWS write.

## Validation

Run:

```bash
bash scripts/validate-v0.11.9.3.4-existing-image-aws-dev-promotion-execution.sh
bash scripts/validate-ci-quality-gates.sh
```

Both are repository validation. Neither command accesses AWS or Kubernetes.
