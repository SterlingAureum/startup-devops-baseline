# v0.11.9.3.5.1 aws-dev live-rehearsal preflight execution

## Outcome

The reviewed v0.11.9.3.5 preflight ran from exact protected-main commit:

```text
cd5aac1f2ab4363fe05ac3507d2abe0a15f54422
```

It returned `ready-for-separate-aws-dev-create-approval`. No rehearsal EKS
cluster was active, and the local dev Terraform state contained zero resource
blocks and zero resource instances. This result permits preparation and review
of a separate creation plan; it does not authorize creation or direct use of
`scripts/apply-aws-dev.sh`.

## Redacted evidence record

The raw private plan and preflight result remain outside the repository. The
repository records only their SHA-256 fingerprints:

```text
plan      56eef1a83217d9d5b2287ac295dcc0fa915313b3d23f3f494d20ab56b151a66b
preflight ad91ef09cd481b9d32efbfb2fbe91ca8e552827bda369f063bf9e02fc499e068
```

The plan parent and evidence directory were mode `0700`; both JSON files were
mode `0600`. The AWS account ID and raw responses are deliberately absent from
the committed record.

## Checks completed

The execution checked:

- private plan permissions;
- a clean `main` equal to `origin/main` and the reviewed plan;
- candidate source ancestry and exact aws-dev release identity;
- STS caller identity against the private expected account;
- the three named rehearsal clusters through EKS inventory;
- a redacted, direct parse of local dev Terraform state.

Only `aws sts get-caller-identity` and `aws eks list-clusters` accessed AWS.
No Terraform command, Kubernetes command, Argo CD command, traffic generation,
environment creation, fault replay, teardown or promotion was executed.

## Main integration note

PR 75 merged v0.11.9.3.5 as protected-main commit `cd5aac1f2ab4`. Main run
`34217132194` was cancelled when the same concurrency group started another
run for that commit; replacement run `34217223349` completed successfully. The
cancellation does not represent a failed validation or a merge-before-checks
condition.

## Next boundary

Keep the private plan and raw result unchanged so their recorded fingerprints
remain reproducible. The next checkpoint must prepare a separate, reviewed
aws-dev creation plan with explicit cost, timing, teardown and evidence
boundaries. This checkpoint still leaves `execution_authorized` false.

aws-test and aws-prod remain blocked. Do not create multiple rehearsal EKS
environments concurrently, and do not replay the local fault scenario in AWS.
