# v0.11.9.3.6.2 aws-dev infrastructure create execution

## Outcome

The separately approved create executor ran once from clean protected main:

```text
b01e76c4155536dd85088bda462d28f49cfde07f
```

It repeated the reviewed read-only preflight byte-for-byte, accepted only a
nonempty Terraform plan containing create/read/no-op actions and completed with
`aws-dev-infrastructure-created-api-ready`. The local dev Terraform state then
contained 103 addresses. The selected application candidate remains
`demo-api-cf0a6bcbc466-cdffd3d71763`.

## Redacted private evidence

No raw private input, Terraform plan, Terraform state or execution log is
committed. The immutable fingerprints are:

```text
create plan             8bceece2ffbe93005730eb314fdce3b1405a305be3746c907ef243b4f5a79a30
fresh preflight plan    903fb208e863837035c0ada19d879fd42fb4560a1067fdc7bd36d6bc6ce1c112
fresh preflight result  f8920484c699c9a3cbfee0fee7c9a804aae306d06b1e4e057dcaed6169cc8a79
verify result            d2899be3a178cd5dd37052b17f3f8424023cbd2f7ea7479238b11383c5387478
execution log            402222e091dd34eba4274d185a4cfcca8ce66db515d9483dc61b27441ca1b505
```

The five private files were mode `0600`; the executor also enforced an
owner-only parent directory. AWS account identity, endpoint CIDRs, node names
and raw resource addresses remain outside the repository.

## Infrastructure observation

At `2026-09-09T08:52:25Z`, the EKS cluster reported:

- name `startup-devops-baseline-dev`;
- status `ACTIVE` and Kubernetes version `1.36`;
- public and private endpoint access enabled, without recording CIDRs;
- four of four nodes Ready on kubelet `v1.36.3-eks-cb19647`;
- final Kubernetes `/readyz` response `ok`;
- no enabled control-plane log types in this cost-bounded rehearsal.

The first separate post-create `/readyz` observation returned a TLS handshake
timeout. The following node inventory succeeded and a read-only `/readyz`
retry returned `ok`. This was a transient observation after the executor had
already completed its API-ready gate; no creation retry or mutation followed.

## Boundary retained

The reviewed session budget was USD 12 with an eight-hour maximum and a
teardown-review deadline of `2026-09-09T15:30:00Z`. Creation used the three
environment confirmations and the fourth interactive Terraform confirmation.

Argo CD was not bootstrapped, its namespace was absent, and no Root Application,
workload, traffic, runtime qualification, remote fault, aws-test promotion or
teardown ran. The environment remains live and billable. Preserve the private
evidence and Terraform state without rerunning the create executor.

The next checkpoint must separately review and implement GitOps bootstrap from
the later protected-main commit. This evidence record does not authorize that
operation or teardown.
