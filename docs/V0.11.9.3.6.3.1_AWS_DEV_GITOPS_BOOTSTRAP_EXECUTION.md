# v0.11.9.3.6.3.1 aws-dev GitOps bootstrap execution

## Outcome

The separately approved `.3.6.3` executor ran once from clean protected main:

```text
64fe6bb58bb5dd6c33a03f25f0df19bef177c825
```

Its pre-execution verification accepted the reviewed AWS account, ACTIVE EKS
1.36 cluster, matching kubectl endpoint, `/readyz=ok`, exactly 103 Terraform
state addresses, absent Argo CD namespace and valid account-bound bootstrap
outputs. The executor then completed with:

```text
aws-dev-gitops-bootstrap-complete
```

## Post-bootstrap convergence

At `2026-09-09T11:16:55Z`, the AWS Load Balancer Controller Application was
`Synced / Healthy` on chart `1.14.0` in `kube-system`, and its Deployment had
completed rollout.

Seven Argo CD Deployments/StatefulSets were observed and all seven had their
reported replicas Ready. The five core Argo CD workloads used
`quay.io/argoproj/argocd:v3.5.2`. The exact auxiliary images were:

- Dex `ghcr.io/dexidp/dex:v2.45.1`;
- Redis `public.ecr.aws/docker/library/redis:8.2.3-alpine`.

The `aws-load-balancer-controller` and `karpenter` ServiceAccounts were both
present, and each role annotation belonged to the reviewed AWS account. The
account ID and raw role ARNs were not printed in this evidence.

## Privacy boundary

No AWS account ID, role ARN, VPC ID, EKS endpoint, Terraform address list,
executor log or raw kubectl inventory is committed. This record contains only
redacted pass/fail facts and non-secret component identities.

## Root boundary retained

The `startup-devops-aws-dev-root` Application was queried with
`--ignore-not-found` and produced no output. Therefore the bootstrap did not
deploy Root or any workload tree. It also did not qualify demo-api, generate
traffic, replay a fault, promote aws-test or tear down aws-dev.

Do not invoke `scripts/deploy-aws-dev-root-app.sh` directly from this evidence
checkpoint. That entrypoint performs many Kubernetes and GitOps writes and
must first receive a `.3.6.4` verify/execute wrapper with exact-main, account,
state, Argo CD convergence, release identity and separate confirmation gates.

## Offline validation

```bash
bash scripts/validate-v0.11.9.3.6.3.1-aws-dev-gitops-bootstrap-execution.sh
bash scripts/validate-ci-quality-gates.sh
git diff --check
```

These commands inspect committed contracts and execute mocked predecessor
tests. They do not access AWS, Terraform, Kubernetes or Argo CD, and they do
not authorize Root deployment or teardown.
