# v0.11.9.3.6.4.2 aws-dev monitoring convergence execution

## Outcome

This checkpoint records the successful, separately approved execution of the
`.3.6.4.1` aws-dev monitoring convergence repair on exact protected-main commit
`6421e140b3ac9a89e806896ef627b258ac201e3b`.

The preflight identified exactly the reviewed recoverable state:

- EKS `1.36` was ACTIVE and `/readyz` returned `ok`;
- Root and demo-api were `Synced / Healthy` on the reviewed commit;
- the aws-dev `Deployment/demo-api` and CloudNativePG cluster were Ready;
- `monitoring-aws-dev` was `Synced / Degraded`;
- the independent Grafana Secret was absent and Grafana was unavailable;
- preflight itself authorized no execution.

## Executed scope

The operator separately approved only the aws-dev monitoring convergence
repair. The guarded executor created the missing independent
`observability-grafana-admin` Secret through the existing privacy-preserving
entrypoint, then observed the Grafana Deployment and monitoring Application
until both converged.

The executor exited `0`. Its private log remains outside the repository and is
bound by SHA-256:

```text
11905ff7f03cc121eda72bcf5ece4db9a303c9496c88ea9fbed331564e9a6e4d
```

The command configured the private log as `0600`; the supplied transcript did
not include a separate later `stat` output, so this checkpoint records the
configuration without claiming an independent mode restatement. No completion
timestamp is invented or committed.

## Accepted postconditions

- the independent Grafana Secret exists, while its values were neither printed
  nor committed;
- Grafana generation `1` was observed at generation `1`, with `1/1` Ready,
  Updated and Available;
- `monitoring-aws-dev` became `Synced / Healthy` on
  `kube-prometheus-stack` `88.5.0`;
- the candidate release remained
  `demo-api-cf0a6bcbc466-cdffd3d71763`;
- Root was not redeployed;
- runtime qualification, traffic, progressive promotion and automatic teardown
  remained unexecuted.

This closes the post-Root monitoring prerequisite. It does not claim runtime or
SLO qualification.

## Offline validation

```bash
bash scripts/validate-v0.11.9.3.6.4.2-aws-dev-monitoring-convergence-execution.sh
bash scripts/validate-ci-quality-gates.sh
git diff --check
```

Validation performs only static inspection and inherited offline tests. It
does not read the private log or access GitHub, AWS, Kubernetes, Argo CD or any
Secret.

## Next boundary

After this evidence reaches protected main, `.3.6.5` may implement a separate
aws-dev runtime qualification preflight and execution boundary. Neither this
evidence nor the prior monitoring approval authorizes that phase.

If the reviewed operational window has expired, stop here. Create a new
continuation window, verify exact current main and live state again, then obtain
separate runtime-qualification approval. If the cluster will remain idle for an
overnight or uncertain period, explicitly compare the continuing EKS, EC2,
NAT, load-balancer and storage cost with the separately reviewed teardown path.
