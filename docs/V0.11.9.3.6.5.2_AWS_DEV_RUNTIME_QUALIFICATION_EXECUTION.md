# v0.11.9.3.6.5.2 aws-dev runtime qualification execution

## Outcome

This checkpoint records the successful, separately approved aws-dev runtime
qualification on exact protected-main commit
`489c8036b21ee465160a1de0973a24b95b73dbfb`.

The fresh post-repair preflight exited `0` and accepted:

- ACTIVE EKS `1.36` with Kubernetes `/readyz=ok`;
- Root, demo-api and monitoring at `Synced / Healthy`;
- the Ready immutable demo-api Deployment and selected Pods;
- the Ready CloudNativePG database and Grafana Deployment;
- the expected Prometheus demo-api target and SLO rule inventory;
- release `demo-api-cf0a6bcbc466-cdffd3d71763`;
- 12,911 seconds remaining before the reviewed UTC deadline.

Preflight authorized no execution, generated no traffic and claimed no runtime
qualification. Its private result remains outside Git and is pinned by
SHA-256:

```text
6f03faaa8d2d5e398cf41f1431a5ba430eebd2c8dc7e137baa078aec63269353
```

## Approved and executed scope

The operator separately approved one aws-dev runtime qualification before
`2026-09-10T14:48:29Z`. The permitted scope was limited to at most 54 normal
requests across `/health`, `/ready` and `/version`, Prometheus SLO and alert
validation, and one final runtime health check.

The guarded executor exited `0` after exactly 54 bounded requests. It accepted:

- a populated release-scoped request series;
- the availability SLO threshold;
- the latency SLO threshold;
- no firing critical aws-dev alert;
- a healthy final demo-api, database and Grafana runtime.

The private successful result remains outside Git and is pinned by SHA-256:

```text
2d56815b00bbcd050966b761828a093ee9fee7ab0aa63b15c77ebfa967c07d61
```

Both stderr files were empty. Their standard empty-file SHA-256 is:

```text
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
```

The evidence directory mode was independently restated as `0700`; the result
and stderr files were independently restated as `0600`.

## Preserved boundaries

This execution did not:

- inject a fault or synthetic error traffic;
- mutate Root or monitoring;
- create a Rollout or AnalysisRun;
- progressively promote a workload;
- promote the release to aws-test;
- execute automatic or manual teardown.

No completion timestamp is inferred from file metadata or invented. Account
IDs, EKS ARNs, endpoints, private paths and raw evidence are not committed.

## Offline validation

```bash
bash scripts/validate-v0.11.9.3.6.5.2-aws-dev-runtime-qualification-execution.sh
bash scripts/validate-ci-quality-gates.sh
git diff --check
```

Validation inspects committed summaries and inherited offline contracts only.
It does not read the private files, access AWS/Kubernetes/Prometheus or produce
traffic.

## Next boundary

This checkpoint closes the aws-dev live runtime qualification. It does not
authorize the next environment.

The remote rehearsal sequence next requires a separately reviewed
aws-dev-to-aws-test release-only promotion. Preserve the aws-dev evidence before
the later separately approved teardown, and continue to enforce the one-active-
EKS-environment cost boundary. Neither this evidence nor the completed aws-dev
approval authorizes an aws-test cluster create, deployment, traffic,
qualification, promotion or teardown.
