# v0.11.9.3.6.6.5.2 aws-dev teardown execution evidence

## Outcome

This checkpoint records the successful, separately approved aws-dev teardown
on exact protected-main commit
`0a90e86ca84498b1d639bbb009491f72e5142454`.

The reviewed read-only preflight was stored outside Git with directory mode
`0700` and result-file mode `0600`. Its SHA-256 was:

```text
98dc729f588f0167f7c89ce19d3c978232855c7e477576b3b1b464fd65b0ae7a
```

It authorized no execution, teardown or mutation. The execution window ran
from `2026-09-11T05:26:01Z` through `2026-09-11T08:26:01Z`.

## Fail-closed input rejection

The first execute invocation omitted the separate immediate-preflight
confirmation. The executor exited `1` before the preflight runner could access
AWS and before the destroy wrapper could be invoked. It performed no mutation
and did not consume the approved teardown.

After the missing confirmation was supplied, the same reviewed preflight,
commit, fingerprint and UTC window were reused. No approval scope was widened.

## Approved and executed scope

The operator approved one aws-dev teardown, including permanent deletion of
the disposable EKS, VPC, compute, storage and ALB resources and the
CloudNativePG backup bucket's base backups, WAL archives, current objects and
historical versions.

The guarded executor accepted a byte-equivalent immediate preflight with 3,481
seconds remaining, invoked the existing aws-dev destroy wrapper once and
exited `0`. Terraform reported 90 destroyed resources, post-success dependency
convergence passed, and the wrapper completed Karpenter Instant Fleet record
retirement. Aggregate terminal evidence observed eight Fleet records: four
delete requests and four records already terminal. No resource identity is
committed.

The private successful result was independently restated as mode `0600` and is
bound by SHA-256:

```text
4801d61d55a08af08ecec4469e64b8f215254b9fdc0eff5d89ba984b6b8c76cf
```

## Preserved boundaries

The approved execution did not create aws-test, perform an automatic retry or
run the residual-cost audit. The wrapper's residual-cost command was printed
only as a next-step handoff; it was not executed.

Raw files and private paths remain outside Git. Account IDs, ARNs, endpoints,
VPC, volume and Fleet identities are not committed. No completion timestamp is
derived from file metadata or invented.

Applying and validating this evidence package performs no AWS, Terraform,
Kubernetes or environment operation.

## Offline validation

```bash
bash scripts/validate-v0.11.9.3.6.6.5.2-aws-dev-teardown-execution-evidence.sh
bash scripts/validate-ci-quality-gates.sh
git diff --check
```

Validation inspects committed summaries and inherited offline contracts only.
It does not read the private files or access AWS, Terraform state or
Kubernetes.

## Next boundary

This checkpoint closes the reviewed aws-dev teardown execution. It does not
claim that the account is free of residual billable resources.

The next phase is an independently reviewed, read-only aws-dev residual-cost
audit. That audit must pass before a new aws-test creation window is designed
or approved. This evidence authorizes neither the audit nor aws-test creation,
deployment, traffic, qualification, promotion or teardown.
