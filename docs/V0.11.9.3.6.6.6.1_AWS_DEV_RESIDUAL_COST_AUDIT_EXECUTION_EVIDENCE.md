# v0.11.9.3.6.6.6.1 aws-dev residual-cost audit execution evidence

## Outcome

This checkpoint records the successful, separately approved, read-only
aws-dev residual-cost audit on exact protected-main commit
`0ae04e26188ab55dc1c76f7d508b40856e798aa5`.

The audit passed without finding any continuing cluster, network, compute,
volume, load-balancer, bucket, certificate, DNS or tagged-resource cost
identity. It performed no mutation, environment operation, automatic retry or
aws-test creation.

Applying and validating this evidence package performs no AWS, Terraform,
Kubernetes or residual-cost-audit operation.

## Reviewed inputs

The private preflight passed on the same exact main commit with no active
rehearsal EKS environment, a readable Terraform backend, zero aws-dev state
resources, no backup bucket and no live or tombstoned runtime secret. Its
redacted result is bound by SHA-256:

```text
e2e3de522b20e72e7eabd8d0f47412fbcd0a1a5a347d152b42111d1333cf38e3
```

The separate executor verify reran a byte-equivalent immediate preflight and
left execution unauthorized. Its redacted result is bound by SHA-256:

```text
fd9fdf11202e13718a08dd7d59853848a6607ac16ab2d5805c6f7668367b2655
```

Both stderr files were empty. The private directories and result files were
independently restated as `0700` and `0600` respectively.

## Approved and executed scope

The operator approved one read-only aws-dev residual-cost audit from
`2026-09-11T09:07:56Z` through `2026-09-11T13:07:56Z`, bound to the exact
commit and the two reviewed fingerprints above. AWS mutation, Terraform
apply/destroy, environment creation or teardown, aws-test creation and
automatic retry were explicitly excluded.

The guarded executor accepted a matching immediate preflight with 14,039
seconds remaining, invoked the historical audit once and exited `0`. The
audit exit code was `0`, both required success markers were present, and the
redacted result reported `continuingCostIdentityFound=false`.

Eight terminal or expired EC2 Fleet records were accepted by the audited
policy. This aggregate count does not represent an active or continuing cost
identity, and no Fleet identity is committed.

The redacted execution result is bound by SHA-256:

```text
6b59e5700a9b24e5f23a26d428e19aca13f80363ea6e32d10931f756613efe29
```

## Private evidence

Raw audit stdout contained 2,399 bytes and is retained outside Git as a
`0600` file. Its SHA-256 is:

```text
0ff1b8033a18c449cf0fc228e643a549657c41f5fea3619cf9c80b60356d91e3
```

Raw audit stderr and executor stderr were empty. Their SHA-256 is the standard
empty-file digest:

```text
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
```

Raw files and private paths remain outside Git. Account IDs, ARNs, endpoints,
VPC, volume, Fleet and other AWS resource identities are not committed. No
completion timestamp is inferred from file metadata or invented.

## Offline validation

```bash
bash scripts/validate-v0.11.9.3.6.6.6.1-aws-dev-residual-cost-audit-execution-evidence.sh
bash scripts/validate-ci-quality-gates.sh
git diff --check
```

Validation checks only committed summaries, fingerprints and inherited mock
contracts. It does not read private files or access AWS, Terraform state or
Kubernetes, and it does not rerun the audit.

## Next boundary

This checkpoint closes the separately approved aws-dev teardown and
post-teardown residual-cost audit chain. It does not authorize aws-test
infrastructure creation.

The next phase is to design and review a fresh exact-main aws-test live
creation preflight, plan and bounded execution window. aws-test creation,
GitOps bootstrap, runtime qualification, promotion and teardown remain
separate operational checkpoints.
