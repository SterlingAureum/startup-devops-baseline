# v0.11.9.3.6.7.2.1 aws-test Terraform plan execution evidence

## Status

The separately approved aws-test plan-only operation completed successfully on
protected main `845d918bbf27`. It produced and machine-gated a private saved
Terraform plan; it did not apply infrastructure or create aws-test.

Applying or validating this evidence checkpoint performs no AWS, Terraform,
Kubernetes, GitOps or traffic operation.

## Reviewed evidence chain

The plan execution is bound to these redacted fingerprints:

| Evidence | SHA-256 |
| --- | --- |
| fresh exact-main preflight | `7a3212227faca5809e5ab4d0313d5cf2ed94d50596bf9fdca41d7b4a1389b0b7` |
| private creation plan | `3d1dec2c651de87053189cfc3e755b24e95d109cdc2027a66b0f2d70420a1237` |
| offline plan-check result | `1513b16a538d3319dc17669a6cf6ff473b1f89eb53451565db495d56df1150fd` |
| executor verify result | `67610e394c2242e336ef0bb572ed4153125e4e519d8df6c29cf27abd521424b1` |
| executor execution result | `513c807b7e5077d9702249580077f791fd2f8d35da9e0bc23b83fa7e9fda846e` |

The preflight, checker and executor stderr files were empty. Private files were
`0600` beneath owner-only `0700` directories. Paths, AWS account, management
address, ARNs, Terraform addresses and raw plan values remain outside Git.

## Approval and execution boundary

The approved UTC session ran from `2026-09-12T08:06:18Z` to
`2026-09-12T16:06:18Z` with a reviewed USD 12 ceiling. Approval covered one
read-only AWS identity/environment/Secret metadata sequence and Terraform
init/plan/show. It excluded apply, destroy, AWS mutation, environment creation,
GitOps, traffic, qualification, promotion, teardown and automatic retry.

The executor exited `0`. The fixed Secret name returned the expected
`ResourceNotFoundException` before and after planning. This proves name
absence for this plan attempt without reading a value or changing Secret
state.

## Machine and human plan review

The accepted plan contains:

```text
90 create
6 read
0 no-op
0 update
0 delete
0 replacement
```

It creates exactly one aws-test EKS cluster. The two EKS access entries are the
expected Karpenter `EC2_LINUX` node entry and the separately bounded GitHub
Actions runtime entry. Five IAM policy documents and one TLS certificate are
read-only data lookups.

The human review at `2026-09-12T08:26:19Z` accepted the expected aws-test
network, EKS, node, IAM, Karpenter/FIS, S3 backup, Secret, certificate, DNS and
supporting resource types. No update, delete, replacement, unexpected
environment or unexpected cost scope was reported.

The private plan artifacts are pinned without publishing their contents:

| Artifact | SHA-256 | Bytes |
| --- | --- | ---: |
| saved binary plan | `f2165bf95e16988a86d314743053c641592151c1e2b3602ca90bf4519b2046e5` | 47319 |
| Terraform JSON | `94205d256d2918177f66d914576a63c0759b4068d2b85e50a0734ad22dd2cc66` | 300637 |
| Terraform text | `152bf76a977a1636706987df4210c6db085f17bc7c1aa2f2c40d87d84ee1c561` | 118707 |
| plan gate | `acf67d2fc1cbf81a06d92b3d774446d5c5224f9fd0069d0e377fa11dc03b6cce` | 347 |
| plan record | `8f076dfe988cad9ab24b9d7ae565204cfc1072934b527b02b29a277f43d9f604` | 1327 |

## Expiry and successor rule

The reviewed saved plan expired at `2026-09-12T09:19:02Z`. It is evidence, not
an apply candidate. Do not edit its timestamp or pass its binary to a later
executor.

Merging this evidence changes protected main. The apply-executor checkpoint
must therefore require all of the following after its own merge:

1. a new clean exact-main SHA;
2. a new byte-reviewed aws-test preflight;
3. a new populated private creation plan and current pricing/window review;
4. a new plan-only execution and human review;
5. apply verification of the fresh saved-plan bytes and unchanged state;
6. a separate explicit apply approval.

The next implementation may reuse the reviewed plan and artifact shapes as
design evidence, but it must not reuse their expired live bytes.

## Offline validation

```bash
bash scripts/validate-v0.11.9.3.6.7.2.1-aws-test-terraform-plan-execution-evidence.sh
bash scripts/validate-ci-quality-gates.sh
git diff --check
```

No live operation is authorized by this document or its validator.
