# v0.11.9.3.6.7.3.1.1 aws-test apply and resume execution evidence

## Status

The separately approved aws-test saved plan was applied exactly once on
protected main `1376129d42c`. Terraform created the 90 reviewed resources and
returned successfully. The `.7.3` wrapper then stopped during its own
post-apply state-address classifier, so its process exit was `1` even though
the infrastructure operation had succeeded.

The `.7.3.1` repair was merged on protected main `8b039bddbb56`. Its local-only
verify accepted every planned create, rejected additional managed state, and
classified the seven extra addresses as the exact reviewed read-only provider
lookups. A separately approved resume then completed the remaining AWS
read-only checks without running Terraform again.

Applying or validating this evidence checkpoint performs no live operation.

## Original plan and apply

The human- and machine-reviewed plan contained:

```text
90 create
6 read
0 no-op
0 update
0 delete
0 replacement
```

The private saved-plan evidence is pinned as follows:

| Artifact | SHA-256 | Bytes |
| --- | --- | ---: |
| saved binary plan | `7c8b5c17b17dcface9fbc97810ec1cedc1985bcb32f3157f4e476b8211d82d0e` | 47323 |
| Terraform JSON | `d93343ca8b7294537eeccff8797827c8af3a63ea6f0088a7dc101236dbf231f3` | 300637 |
| Terraform text | `152bf76a977a1636706987df4210c6db085f17bc7c1aa2f2c40d87d84ee1c561` | 118707 |
| plan gate | `acf67d2fc1cbf81a06d92b3d774446d5c5224f9fd0069d0e377fa11dc03b6cce` | 347 |
| plan record | `a196b0b7370d7c3772c3a4a60ebeff43fd39ddd59aa7921fa8afabdefab98852` | 1327 |

The apply wrapper result file was empty because it stopped before emitting its
redacted JSON. Its stderr fingerprint is
`410415ad92580ecd0650655776821821caf6653bbc57cb4dd70f63995760be4e`.
The captured Terraform apply stdout fingerprint is
`ca2e3d7e1884e40a0339ce7912dae61d28be9abb720b0e3262df5e93a446e046`;
Terraform apply stderr was empty. No retry, replan or second apply occurred.

## State classification

The resulting owner-only local state remained unchanged throughout the
repair and resume. Its SHA-256 is
`a53f7bb1091990195680c9b3918a6110d4b3b8cb61441ce22aec2faf68fa725b`.
The reviewed aggregate is:

```text
96 plan resource changes
90 planned create addresses
103 state addresses
80 state resource blocks
103 state resource instances
0 missing planned creates
0 unexpected managed addresses
7 accepted unplanned data addresses
0 unclassified addresses
```

The seven accepted entries are the exact three caller-identity, three
partition and one DNS-zone provider lookups recorded by `.7.3.1`. They are not
seven additional AWS resources. Arbitrary data entries and every additional
managed address remain rejected.

## Read-only resume

The local resume input verification exited `0` with result SHA-256
`baf9dd1b887682dd7e475f1b58317c87a5bc26da21fb81073c8a8087a8a63051`.
It executed no command and left resume, plan, apply and destroy unauthorized.

The subsequent separately approved execution performed only:

- caller-identity verification;
- cluster inventory verification;
- aws-test cluster ACTIVE and reviewed API-boundary verification;
- credential-container metadata verification without reading its value.

The resume executor exited `0`. Its redacted result SHA-256 is
`b9f240afa6df38167cf2e3c5abcc8a7db583afa9a59488f3c3cbe6b9250d183f`,
and its stderr was empty. It confirmed the prior apply, 90 planned creates,
zero missing creates, zero unexpected managed addresses, seven accepted data
lookups, 103 total state addresses, an ACTIVE aws-test cluster and present
metadata.

The four private stdout fingerprints are:

| Read-only output | SHA-256 | Bytes |
| --- | --- | ---: |
| identity response | `1e19721c43738416dcf7e781f5b55858cc70ab7024693eb18f060dea10f18d94` | 197 |
| inventory response | `9e95155afd2fcca6cd7eebc4158020b8b32800ac42948a6237f9b80949615ef9` | 67 |
| cluster response | `f1de181f9e03d379a819468ea6b3f5df3774cf0a32f26eff8cae66980515888f` | 4225 |
| metadata response | `b56815ad21d9c784844929c040d9ec27a83ad016bdfff7405798801b5ae3a1ac` | 1099 |

All four stderr files were empty. Every private file was `0600` beneath an
owner-only `0700` directory.

## Boundaries and privacy

This checkpoint records that the original approved plan created aws-test. It
does not authorize another apply. The resume ran no Terraform command and no
AWS mutation. It did not alter state, bootstrap GitOps, generate traffic,
qualify the release, promote it or tear down the environment.

Raw AWS output, local state, private paths, account identity, management
address, ARNs, resource identities and credential values remain outside Git.
Only redacted facts, aggregate counts and SHA-256 fingerprints are committed.

## Next checkpoint

The next phase may design a guarded aws-test GitOps bootstrap. It must begin
from the new post-evidence protected-main SHA and use a fresh preflight,
separate review and explicit execution approval. This evidence package itself
does not authorize GitOps access, cluster mutation, traffic or qualification.

Offline validation:

```bash
bash scripts/validate-v0.11.9.3.6.7.3.1.1-aws-test-apply-and-resume-execution-evidence.sh
bash scripts/validate-ci-quality-gates.sh
git diff --check
```
