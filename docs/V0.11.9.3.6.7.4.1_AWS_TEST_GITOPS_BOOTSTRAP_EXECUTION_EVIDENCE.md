# v0.11.9.3.6.7.4.1 aws-test GitOps bootstrap execution evidence

## Status

The separately approved aws-test GitOps platform bootstrap completed once on
protected main `4aa621267676`. Exact Argo CD `v3.5.2`, the two reviewed IRSA
ServiceAccounts and the AWS Load Balancer Controller child Application were
created. The aws-test Root Application remained absent.

Applying or validating this evidence checkpoint performs no AWS, Terraform,
Kubernetes, Argo CD, GitOps or traffic operation.

## Preflight and isolated kubeconfig

The first verifier run stopped safely because the active kubeconfig did not
target the reviewed aws-test EKS endpoint. It emitted no result, performed no
mutation and did not authorize execution. Its redacted stderr fingerprint is
`0296b157282a7b9c98cb2df702568da1127766a65b8008623ae5be376e15b6f1`.

An owner-only isolated kubeconfig was produced with AWS CLI dry-run behavior;
the default kubeconfig was not modified. Its initial SHA-256 was
`9e8bdb782ccc8a368b7e64f64abe7bfa51307f0d9855cb1c4b505cd05968f9b2`.
The new manual verify then passed with result SHA-256
`e56d86d139d51c93c417de6d1891ff98cdbc4141219104f4ba922d51e2306bdc`
and empty stderr. It confirmed ACTIVE aws-test EKS, the reviewed API boundary,
103 state addresses, Kubernetes readiness and absence of the Argo CD namespace
and Root Application while leaving bootstrap unauthorized.

## Approval and execution

The explicit approval bound the exact protected main, original infrastructure
apply commit, `.7.3.1.1` evidence, Terraform state, isolated kubeconfig,
successful verify and shared-bootstrap fingerprints. It allowed exactly one
shared bootstrap invocation and only the reviewed platform mutations. It did
not authorize Terraform mutation, the Root Application, other AWS/Kubernetes
mutation, credential-value reads, retry, repair, traffic, qualification,
promotion or teardown.

The executor exited `0`. Its redacted result is 1003 bytes with SHA-256
`ad674765975674e1f5aa0abad2cb6bbccd17b5c78cf868b8cffe4a098d4bf80a`;
executor stderr was empty. The isolated kubeconfig was updated only by the
approved bootstrap and its final fingerprint is
`b1f390903139c1d49a76bf5afcfdad892c2c6a7828310a3ecf791efd6808cd0f`.
The Terraform state remained byte-identical at
`a53f7bb1091990195680c9b3918a6110d4b3b8cb61441ce22aec2faf68fa725b`.

## Private evidence

The private output contains 62 owner-only files totaling 174294 bytes. All
files were `0600` beneath a `0700` directory. Raw content and paths remain
outside Git.

The shared bootstrap stdout is 4969 bytes with SHA-256
`e1004e6cea91de51abb370a37c398ce91e584ad408c8257b6989de995c0c9142`.
Its stderr is 193 bytes with SHA-256
`f9d72d2ce3318e9381de39d14d85fba0e3ce452ce9b68d721a51e4d21242dc78`.
The operator separately classified that stderr as exactly one warning line,
zero other lines and no failure marker. All executor checks and post-checks
passed, so it is recorded as reviewed non-fatal warning evidence rather than a
hidden failure.

The before/after caller identity, cluster inventory and description, Secret
metadata, Terraform state list, Kubernetes readiness and Terraform-derived
role outputs matched. Post-bootstrap evidence confirms:

- the Argo CD namespace;
- ready Argo CD server, repository server and application controller;
- both reviewed ServiceAccounts with exact Terraform-derived role annotations;
- the AWS Load Balancer Controller child Application;
- continued absence of the aws-test Root Application.

## Boundaries and next checkpoint

This checkpoint records one completed platform bootstrap. It does not authorize
another bootstrap or the Root Application. Terraform state was not mutated,
and no Terraform plan/apply/destroy, credential-value read, traffic,
qualification, promotion, teardown, automatic retry or repair occurred.

The next phase may design a guarded aws-test Root Application deployment. It
must start from the new post-evidence protected-main SHA, run a fresh private
preflight and require separate explicit approval.

Offline validation:

```bash
bash scripts/validate-v0.11.9.3.6.7.4.1-aws-test-gitops-bootstrap-execution-evidence.sh
bash scripts/validate-ci-quality-gates.sh
git diff --check
```
