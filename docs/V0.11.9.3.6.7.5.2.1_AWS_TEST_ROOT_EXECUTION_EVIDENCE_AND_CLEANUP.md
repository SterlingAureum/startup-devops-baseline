# v0.11.9.3.6.7.5.2.1 — Root execution evidence and cleanup preparation

Application baseline: protected main `375417830174723598a2565b8f197f74c2c50697`.
This increment records the operator-confirmed, separately approved deployment
and adds an exclusively read-only cleanup dependency observer. Creating,
applying or validating this increment does not deploy again or authorize any
resource deletion. No producer AWS/Kubernetes/Terraform access is performed.

## Completed deployment

The local prepare and human manifest/cost review were followed by a successful
fresh verify at `2026-09-13T07:39:44Z`, fingerprint
`af7ac34f61bf57d944ceef47a88cd7d30f58b676e5580e69f565db9dee7d76d5`.
The operator separately approved the exact private plan and immutable Root,
two namespaces/IRSA accounts, declared controller scope, initial credential
transfer/readback, test ExternalSecret refresh and the owned test DNS alias.
The verify permitted starting once before `07:54:44Z`; this was a start-proof
TTL, not an instruction to stop an already-started approved run at that time.
The execution enforced its 90-minute maximum and absolute `09:21:13Z` stop.

The operator collected completion evidence at `2026-09-13T08:03:11Z`:

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| Execution result (outer and internal identical) | 821 | `7b645bbf7ef954a886111bf4a4431b4627bf6f938d3d0c95d46af2713622a68c` |
| Execution stderr | 0 | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| Mutation-attempt journal | 1149 | `7f00e5b4c312dd0875062a459a6939aa9b733e2d83d79157cf72c3ad33c32c9f` |
| One-time marker | 195 | `57602d7c83888d647576693e3b4d37b2311ca843c191ae9f3d4b5bc6d866015f` |
| Unchanged Terraform state | 241960 | `a53f7bb1091990195680c9b3918a6110d4b3b8cb61441ce22aec2faf68fa725b` |

All listed file modes were reported as 0600. Directory protection is enforced
by the executor; no new independent directory-stat result was supplied with
completion. Raw API/Secret command output contents and per-command fingerprints
were not supplied for public recording. They remain private; do not invent
their hashes or infer that every command stderr was empty from the outer file.
The sanitized result's full field map is recorded, and its 821-byte canonical
JSON representation reproduces the supplied hash exactly.

The result reports Root and demo Application **Healthy**, database ready,
credential seed/readback/ESO consistency successful, DNS INSYNC and all nine
same-repository children pinned. State is byte-identical. Traffic,
qualification, automatic retries and automatic teardown were not executed.
Healthy Argo objects do not establish HTTP/database runtime acceptance or a
Qualification Bundle. The Root remains pinned to the deployment commit after
this evidence-only successor merges; do not update live revisions to the new
main as part of evidence recording.

The consumed approval, old private plan and attempt marker remain historical
proof, not permission to execute again. Preserve the persistent private bundle,
recovery inputs, isolated kubeconfig, state and all command output. Lost
original temporary creation-plan bytes are not needed or reconstructed.

## Budget and deadline

The revised total envelope is USD 36, with USD 20 an expectation. Preparation
estimated USD 26.99 and verify USD 26.88 through cleanup. These are planning
estimates; historical attributable billed spend remains unknown. No new current
billing or cost estimate is asserted by this evidence increment.

**Cleanup must finish by `2026-09-13T10:51:13Z`.** The read-only observer runs
for at most ten minutes and stops by `09:21:13Z`, retaining the previously
reviewed 90-minute cleanup reserve. Inventory taken shortly before that stop
may receive less than ten minutes and stop safely. No deadline extension,
automatic teardown or billing cap is provided. Controllers/cloud work can
continue while no operator command is running. An expired or insufficient
window requires a new explicit recovery/cleanup decision.

## Why the existing wrapper cannot be called directly

The test wrapper selects aws-test but delegates to the shared dev destroy file.
The reviewed source updates the default kubeconfig, pauses only Root and
PostgreSQL automation before deleting shared resources, calls dynamic AWS
dependency convergence and may invoke Terraform destroy again after a failure.
It has per-stage waits without this session's absolute UTC deadline. These
findings are recorded with source fingerprints. Do not edit pinned legacy files
or set their confirmation to bypass a separately reviewed cleanup executor.

## Fresh read-only inventory after this increment merges

Run the new offline gate, obtain the exact newly merged clean main SHA, then
reuse the persistent recovery inputs, consumed deployment bundle and original
successful deployment result. The observer validates those original hashes
without requiring the historical private plan to be unexpired: it is evidence,
not a new deployment plan. The live Root/child sources must still match the
original deployment manifest and source map, even though current main advanced.

Unset all three Root/credential/DNS execution confirmations and the old
apply/destroy/bootstrap confirmations. The tool checks exact local and remote
main, account, EKS inventory/ACTIVE/VPC/API boundary and isolated kubeconfig auth.
It reads Applications/in-flight operations, Nodes, NodePools, NodeClaims,
EC2NodeClasses, PostgreSQL Clusters, PV/PVCs, VPC instances and attached disks,
owned ALB/DNS, credential-container metadata and S3 object versions/delete
markers. It never reads credential values or object bodies. Unexpected
Applications, sources, PVC namespaces, ALB/DNS ownership or identity cause stop.

Only explicit AWS metadata/list/describe actions and Kubernetes get/config-view
are allowed at the command-launch boundary. Terraform, shell wrappers,
Kubernetes apply/delete/patch/exec, Secret value reads and AWS writes cannot be
launched by the observer. Raw output is 0600 in a new persistent 0700 directory.
Attached volumes/PVs do not prove an exhaustive orphan-volume inventory, and
S3 version listing is not an atomic snapshot while backups remain running.
The public summary labels those limitations and leaves teardown authorization
false. In-flight sync is reported, never terminated by observation.

After successful merge, in the repository terminal:

```bash
umask 077
export AWS_TEST_CLEANUP_EXPECTED_MAIN='REPLACE_WITH_NEW_MERGED_MAIN_SHA'
export AWS_TEST_ROOT_SESSION_DIR='/absolute/persistent/deployment-session'
export AWS_TEST_ROOT_INPUTS='/absolute/persistent/recovery-inputs.json'
export AWS_TEST_CLEANUP_OUTPUT="$AWS_TEST_ROOT_SESSION_DIR/cleanup-inventory"

python3 scripts/observe-v0.11.9.3.6.7.5.2.1-aws-test-cleanup.py \
  --inputs "$AWS_TEST_ROOT_INPUTS" \
  --expected-main "$AWS_TEST_CLEANUP_EXPECTED_MAIN" \
  --bundle "$AWS_TEST_ROOT_SESSION_DIR/bundle" \
  --deployment-result "$AWS_TEST_ROOT_SESSION_DIR/execution/result.json" \
  --output-directory "$AWS_TEST_CLEANUP_OUTPUT" \
  >"$AWS_TEST_ROOT_SESSION_DIR/cleanup-inventory-result.json" \
  2>"$AWS_TEST_ROOT_SESSION_DIR/cleanup-inventory-result.stderr"
printf 'cleanup_observe_exit=%s\n' "$?"
```

On success, return sanitized summary, current UTC, summary SHA-256 and file
modes. Never paste raw inventories or Secret content. On failure preserve all
output and state; stop without automatic retry/repair. Reusing an existing
output directory is refused. No destroy plan is produced by this observer.

## Required independent deletion workflow

The next implementation must bind exact main, state snapshot, private runtime
inventory, consumed deployment proof and the fixed cleanup end before deletion.
It needs a separate human review and destructive approval, explicitly including
CNPG data and every base backup/WAL/current object/historical version that will
be permanently deleted. The observer has not implemented those mutations.

Review the actual private inventory and sequence these dependencies:

1. Preserve state/private evidence. Control in-flight and automated sync for
   every captured Application so children cannot recreate deleted resources.
2. Remove only the exact owned test alias; clean application/CNPG/PVC storage
   while ALB, CNPG and CSI controllers remain available. Account for monitoring
   PVCs and any retained/detached EBS disks, not only attached-volume counts.
3. Drain captured NodeClaims and clean EC2NodeClass instance-profile dependencies
   before removing Karpenter controllers. Complete owned ALB/ENI dependencies
   before removing their controllers/network. Do not delete unrelated resources.
4. Review and apply one exact Terraform destroy plan with its dynamic dependencies
   and original reviewed CIDR input. Do not use a wrapper that silently repairs
   or retries. Failure requires state/private evidence preservation and a new
   read-only review. Terraform state legitimately changes during approved destroy.
5. Verify absent EKS/VPC/compute/EBS/ALB resources, backups and live credential
   container values; classify any expected deletion tombstone or terminal Fleet
   records. Preserve the shared hosted zone and immutable release artifacts.
   A separate residual-cost audit remains necessary for cost closure.

No deletion executor, destroy plan, residual-cost-audit pass, or cleanup approval
is claimed in this increment. If time becomes insufficient, review cleanup
priority and a concrete recovery plan; do not quietly prolong the old window.
