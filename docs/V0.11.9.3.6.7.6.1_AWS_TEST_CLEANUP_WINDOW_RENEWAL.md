# v0.11.9.3.6.7.6.1 — Explicit cleanup-window renewal

Apply on clean protected main `1c1fbca121acfb02b7aec74f349668713598c5d7`.
The user confirmed the proposed new completion deadline after the earlier time
check stopped at September 13 09:26:26Z. That stop preceded any inventory or
mutation. No environment creation, Terraform apply, platform bootstrap or Root
execution is repeated. Historical scripts, contracts, source pins and window
records remain byte-identical. This package itself executes no live command.

## Window

| Boundary | Historical | Confirmed replacement |
| --- | --- | --- |
| Runtime latest start | 2026-09-13T09:01:13Z | 2026-09-13T10:40:00Z |
| Runtime command stop | 2026-09-13T09:21:13Z | 2026-09-13T11:00:00Z |
| Cleanup completion | 2026-09-13T10:51:13Z | 2026-09-13T12:30:00Z |

The new script reads the new fixed contract; it does not alter the host clock or
reuse historical approvals. The reviewed observer copy preserves the historical
Root manifest/source-map/deployment result hashes and deployment commit. Its
execution cutoff comes explicitly from the renewed contract, keeping a 90-minute
cleanup reserve. Runtime phases require twenty minutes remaining; later Terraform
phases require fifteen minutes remaining before completion. A bounded operation
can still exceed the time available and leave an uncertain outcome; preserve all
evidence and state on failure. Controllers and billing may continue after process
termination. Do not automatically retry, repair or extend this window again.

After merge, use the new exact clean protected-main SHA and a new persistent
cleanup workspace. The previous attempted shell time check produced no consumed
runtime marker. Any existing executor attempt marker is retained and never removed
to retry. New runtime proofs, plan proofs and completion receipts bind the new
window contract SHA plus current main, input/state and inventory hashes. A proof
from the old window is refused even if its other fields appear valid.

## Whole-session estimate

The total limit remains USD 36 and USD 20 remains an expectation. The model uses
the existing manually reviewed hourly allowances from .7.5.2, not newly verified
AWS prices. Accounting starts September 12 09:38:53Z; it assumes idle capacity
before September 13 07:39:44Z (the earliest reviewed Root-start boundary), then
peak configured capacity through September 13 12:30:00Z, plus the original USD 3
reserve. The peak envelope covers the configured capacity, including a possible
fifth new c6i.large, rather than assuming early cleanup savings.

This gives **USD 29.55** under that model. It includes the elapsed interval rather
than treating the extension as a new zero-spend session. Actual historical billing
is unknown; CPU credits, network/ALB/backup/log use can differ from allowances, and
this is neither an invoice nor a guaranteed upper bound. Projection above USD 36
stops the new executor. Fresh resource scope and human budget review are still
required before deletion. The projection is captured in sanitized output and the
private runtime proof; setting a review flag cannot renew its timestamp.

## Post-merge read-only verify

Use the persistent recovery inputs, existing isolated kubeconfig, original
immutable Root bundle and completed deployment result. No missing temporary
creation plan is required. The original local state hash remains required until
an independently approved saved deletion plan changes it.

Set the following paths to the existing persistent files. Set the parent to an
existing private directory with mode 0700; the session, workspace and output are
new. Only the new entry point below accepts the renewed window.

```bash
umask 077
export PYTHONDONTWRITEBYTECODE=1
export EXPECTED_CONTROL_PLANE_COMMIT='<new-merged-main-sha>'
export AWS_TEST_RECOVERY_INPUTS='<persistent-inputs.json>'
export AWS_TEST_ROOT_SESSION='<existing-immutable-root-directory>'
unset CONFIRM_AWS_TEST_RUNTIME_CLEANUP CONFIRM_AWS_TEST_TEARDOWN_PLAN
unset CONFIRM_AWS_TEST_EKS_DELETE CONFIRM_AWS_TEST_FINAL_DELETE
unset CONFIRM_AWS_TEST_ROOT_DEPLOYMENT CONFIRM_AWS_TEST_CREDENTIAL_SEED
unset CONFIRM_AWS_TEST_DNS_UPSERT CONFIRM_AWS_TEST_APPLY CONFIRM_AWS_DEV_APPLY
unset CONFIRM_AWS_ENVIRONMENT_DESTROY CONFIRM_AWS_DEV_TEARDOWN_EXECUTION
unset CONFIRM_AWS_TEST_GITOPS_EXECUTION
export AWS_TEST_CLEANUP_SESSION="$(mktemp -d "$AWS_TEST_ROOT_SESSION/renewed-teardown.XXXXXX")"
export AWS_TEST_CLEANUP_WORKSPACE="$AWS_TEST_CLEANUP_SESSION/workspace"
export AWS_TEST_CLEANUP_VERIFY="$AWS_TEST_CLEANUP_SESSION/verify-output"
python3 scripts/execute-v0.11.9.3.6.7.6.1-aws-test-teardown.py verify-runtime \
  --expected-main "$EXPECTED_CONTROL_PLANE_COMMIT" \
  --inputs "$AWS_TEST_RECOVERY_INPUTS" \
  --bundle "$AWS_TEST_ROOT_SESSION/bundle" \
  --deployment-result "$AWS_TEST_ROOT_SESSION/execution/result.json" \
  --workspace "$AWS_TEST_CLEANUP_WORKSPACE" \
  --output-directory "$AWS_TEST_CLEANUP_VERIFY" \
  >"$AWS_TEST_CLEANUP_SESSION/verify.json" \
  2>"$AWS_TEST_CLEANUP_SESSION/verify.stderr"
VERIFY_EXIT=$?
printf 'verify_exit=%s\n' "$VERIFY_EXIT"
sha256sum "$AWS_TEST_CLEANUP_SESSION/verify.json" "$AWS_TEST_CLEANUP_SESSION/verify.stderr"
wc -c "$AWS_TEST_CLEANUP_SESSION/verify.stderr"
cat "$AWS_TEST_CLEANUP_SESSION/verify.json"
if [[ "$VERIFY_EXIT" -eq 0 ]]; then
  stat -c '%a %n' "$AWS_TEST_CLEANUP_WORKSPACE/runtime-proof.json" "$AWS_TEST_CLEANUP_WORKSPACE/runtime-record.json"
  sha256sum "$AWS_TEST_CLEANUP_WORKSPACE/runtime-proof.json" "$AWS_TEST_CLEANUP_WORKSPACE/runtime-record.json"
fi
```

The executable checks its time gate before cloud access. Output must report the
new window SHA, completion at 12:30:00Z, projection USD 29.55 and
`human_reviewed=false`, `teardown_authorized=false`. Do not paste raw records.

## Independent deletion approvals

Use the staged interface and scopes in the
[original teardown procedure](V0.11.9.3.6.7.6_GUARDED_AWS_TEST_TEARDOWN.md), but
replace its executable with .7.6.1 and use the renewed boundaries above. The old
procedure's timestamps and entry point apply only to the historical window.

Runtime: review the private inventory, complete Application/NodePool/NodeClass
specs, exact DNS alias, disk identities and cost projection. Only then set the
runtime-proof human_reviewed flag to true, hash the changed proof and obtain
explicit approval bound to that SHA, state, new main and renewed window. The
proof TTL remains fifteen minutes. Confirmation of this schedule grants no
runtime deletion approval.

Next, separately approve EKS destroy-plan production from runtime-complete.json;
manually review its binary/JSON/text and plan-proof; separately approve that exact
saved plan plus gated unreferenced EKS-created SG removal. Then separately approve
final destroy-plan production from eks-complete.json and its exact saved apply.
Each plan is valid thirty minutes; changing a flag cannot renew it. The existing
phase confirmation names/values are unchanged and only the current one may be set.
Use the workspace completion receipts as proofs, not the outer result JSON.

Permanent backup/version deletion and Terraform-defined credential container
removal remain part of the explicitly reviewed final plan. No credential values
are read. Known Argo finalizers are deliberately removed to orphan chart resources;
storage finalizers are never forced. Dependency controllers remain available for
namespace/storage/NodeClaim/ALB cleanup. No old destroy wrapper is called. Failure
consumes its attempt marker and stops with evidence/state preserved.

A successful final captured-resource check still needs an independent residual
cost audit and execution evidence. No traffic, qualification, promotion or automatic
teardown is added. All raw files stay in persistent private directories, 0700/0600.
