# v0.11.9.3.6.7.6.2 — Partial runtime-cleanup continuation

Application baseline: clean protected main
`a7390ea95be2539e20f37b81a82efebf213f16cd`.
This increment repairs an invalid kubectl output option and verifies a partially
completed cleanup before any separately approved continuation. It preserves all
historical scripts/contracts, original private evidence and consumed markers.
No live cloud/Kubernetes/Terraform command is run by the package producer.

## Confirmed failure

The operation stopped at September 13 10:04:20Z after 21 mutation attempts:
19 Application freeze patches, Root finalizer patch and one Root deletion attempt.
The failed command used -o json. Its stdout was empty; the 66-byte stderr hash
exactly matches the public kubectl error: unexpected -o output mode json; only
-o name is supported. Source code assumed JSON output for delete as for get/patch.

The package's real kubectl 1.36.0 test against a loopback fake API reproduces that
exact error with zero DELETE requests. Using -o name exits zero, prints text and
sends exactly one DELETE request. Tests supply an isolated empty kubeconfig,
fixture token and loopback server; no real account or cluster is contacted.
Actual Root presence still needs fresh observation; do not infer current state
from an earlier command outcome or recreate a missing Root.

The previous tests mocked the delete invocation and checked propagation mode;
they did not validate real kubectl output handling. This increment adds that
missing CLI regression and tests original-marker preservation.

## Confirmed new window and unchanged budget

| Boundary | Previous | Confirmed continuation |
| --- | --- | --- |
| Runtime stop | September 13 11:00:00Z | September 13 12:30:00Z |
| Runtime latest phase start | September 13 10:40:00Z | September 13 12:10:00Z |
| Cleanup completion | September 13 12:30:00Z | September 13 14:00:00Z |

All dates are UTC, 2026. The new window keeps ninety minutes after runtime stop
for separately approved EKS/final plans and applies. Runtime phases require at
least twenty minutes remaining at start; Terraform phases require fifteen minutes
remaining before completion. Processes are bounded; controllers and billing can
continue after the executor stops. Missing time or uncertain failure stops the
phase rather than silently extending or automatically retrying it.

Using the previously manually reviewed .7.5.2 allowances, whole-session accounting
starts September 12 09:38:53Z, assumes idle until September 13 07:39:44Z, then peak
configured capacity through 14:00:00Z plus the original USD 3 reserve. Projection
is **USD 31.97** against the unchanged **USD 36** limit. USD 20 is an expectation.
Historical billed spend is unknown and current prices are not newly verified.
This is not a guaranteed quote, invoice or hard billing cap. Over-budget projection
stops execution. Human scope/cost review remains required before continuation.

## Read-only continuation verification after merge

Use the new exact merged main SHA and keep it fixed through all phases. The new
workspace is separate from the original failed workspace. Retain the original
runtime-attempt.json and all raw freeze/orphan/delete outputs unchanged.

Required persistent evidence:

- Existing recovery inputs, isolated kubeconfig and recovery summary.
- Completed immutable Root bundle and deployment result; no lost temporary
  creation plan or infrastructure/platform/Root replay is required.
- Original failed runtime workspace and runtime-output directory, including the
  reviewed original proof, exact runtime-record, failure, marker, 21-entry journal
  and all 19 successful freeze responses plus Root orphan response.
- Byte-identical original Terraform state, mode 0600, until approved EKS deletion.

The verifier checks the published artifact hashes and exact journal sequence;
binds Application UIDs from the successful freeze responses; requires all remaining
Applications still paused with matching source/destination/full normalized specs;
rejects restored Root finalizers, unknown finalizers, active operations or deletion
in progress. It accepts either all 19 original Applications or the 18 children
with Root absent. Recreated or extra Applications stop verification.

It also checks target account/EKS/CIDR/kubeconfig, original state resource IDs,
unchanged NodePool/NodeClass specs, five PV/PVC identities, four original NodeClaims,
eight original On-Demand instances, thirteen associated disks totaling 312 GiB,
owned original ALB/test alias and Secret/S3 metadata. No credential value is read.
Backup inventory is non-atomic; new versions are recorded rather than frozen to an
older count. Failure stops with private evidence; no mutation occurs in verify.

Set these paths to the existing persistent evidence and run only the new entry:

```bash
umask 077
export PYTHONDONTWRITEBYTECODE=1
export EXPECTED_CONTROL_PLANE_COMMIT='<new-merged-main-sha>'
export AWS_TEST_RECOVERY_INPUTS='<persistent-inputs.json>'
export AWS_TEST_ROOT_SESSION='<existing-immutable-root-directory>'
export AWS_TEST_PRIOR_CLEANUP='<original-renewed-teardown-directory>'
unset CONFIRM_AWS_TEST_RUNTIME_CLEANUP CONFIRM_AWS_TEST_RUNTIME_RESUME
unset CONFIRM_AWS_TEST_TEARDOWN_PLAN CONFIRM_AWS_TEST_EKS_DELETE CONFIRM_AWS_TEST_FINAL_DELETE
unset CONFIRM_AWS_TEST_ROOT_DEPLOYMENT CONFIRM_AWS_TEST_CREDENTIAL_SEED CONFIRM_AWS_TEST_DNS_UPSERT
unset CONFIRM_AWS_TEST_APPLY CONFIRM_AWS_DEV_APPLY CONFIRM_AWS_ENVIRONMENT_DESTROY
unset CONFIRM_AWS_DEV_TEARDOWN_EXECUTION CONFIRM_AWS_TEST_GITOPS_EXECUTION
export AWS_TEST_RESUME_SESSION="$(mktemp -d "$AWS_TEST_ROOT_SESSION/runtime-resume.XXXXXX")"
export AWS_TEST_RESUME_WORKSPACE="$AWS_TEST_RESUME_SESSION/workspace"
python3 scripts/execute-v0.11.9.3.6.7.6.2-aws-test-teardown.py verify-resume \
  --expected-main "$EXPECTED_CONTROL_PLANE_COMMIT" --inputs "$AWS_TEST_RECOVERY_INPUTS" \
  --bundle "$AWS_TEST_ROOT_SESSION/bundle" \
  --deployment-result "$AWS_TEST_ROOT_SESSION/execution/result.json" \
  --prior-workspace "$AWS_TEST_PRIOR_CLEANUP/workspace" \
  --prior-output "$AWS_TEST_PRIOR_CLEANUP/runtime-output" \
  --workspace "$AWS_TEST_RESUME_WORKSPACE" \
  --output-directory "$AWS_TEST_RESUME_SESSION/verify-output" \
  >"$AWS_TEST_RESUME_SESSION/verify.json" 2>"$AWS_TEST_RESUME_SESSION/verify.stderr"
RESUME_VERIFY_EXIT=$?
printf 'resume_verify_exit=%s\n' "$RESUME_VERIFY_EXIT"
sha256sum "$AWS_TEST_RESUME_SESSION/verify.json" "$AWS_TEST_RESUME_SESSION/verify.stderr"
wc -c "$AWS_TEST_RESUME_SESSION/verify.stderr"
cat "$AWS_TEST_RESUME_SESSION/verify.json"
if [[ "$RESUME_VERIFY_EXIT" -eq 0 ]]; then
  sha256sum "$AWS_TEST_RESUME_WORKSPACE/runtime-proof.json" "$AWS_TEST_RESUME_WORKSPACE/runtime-record.json"
fi
```

Expected status: aws-test-runtime-resume-inputs-verified. human_reviewed and
teardown_authorized remain false; output reports Root presence, 18/19 remaining
Applications, new window SHA, USD 31.97 projection and completed_freeze_repeated=false.
Keep raw records private. All directories are 0700; files/proofs/plans/state are 0600.

## Independent continuation approval

Review the new private runtime-record and cost projection. Only after human review
set its new runtime-proof human_reviewed flag true, preserve the timestamp and hash
the changed proof (a new reviewed copy in the same private directory is allowed).
Obtain explicit approval bound to that new proof, record, state, current main,
window, remaining scope and budget. Runtime proof TTL is fifteen minutes; an old
reviewed proof from .7.6.1 cannot enter this window.

execute-resume uses the same arguments plus --proof and --expected-proof-sha256,
a new output directory, and exactly:
CONFIRM_AWS_TEST_RUNTIME_RESUME=cleanup-reviewed-aws-test-runtime-resume-once.
The old runtime confirmation is forbidden. The new runtime-resume-attempt.json is
exclusive and consumed before immediate fresh checks; the original marker is never
removed or reset. Recheck drift stops the new operation, with no automatic repair.

No freeze patch is repeated. An absent Root is skipped; a present original Root
with no finalizers is deleted once. For remaining children, remove only their
known Argo finalizers when present, intentionally orphan chart resources, then
request deletion with -o name, retaining raw text instead of JSON parsing.

The reviewed runtime dependencies are then cleaned as in .7.6: exact test alias,
three namespaces, five captured PVs and detached disks, two NodePools and three
NodeClasses. Controllers coordinate original NodeClaim/compute/ALB dependencies;
system controllers/nodes, EKS/VPC, backup bucket and credential container remain
for subsequent independently approved stages. Storage finalizers are never forced.

Success writes runtime-complete.json in the NEW workspace and reports
aws-test-runtime-resume-cleanup-complete without Terraform or EKS deletion. Use
that receipt for separately approved plan-eks; review binary/JSON/text and plan-proof
then separately approve apply-eks with strictly gated orphan SG removal. Next
separately approve plan-final and its exact apply-final, including permanent backup
version deletion and Terraform-defined credential container removal. Plan TTL is
thirty minutes. Those stages use this .7.6.2 executable and new window-bound receipts.
No old wrapper, repeated apply, traffic, qualification or promotion is added.

See [the original staged deletion scopes](V0.11.9.3.6.7.6_GUARDED_AWS_TEST_TEARDOWN.md)
for Terraform gates; its old runtime entry and timestamps do not authorize this
continuation. Final captured-resource checks are followed by a separately approved
residual-cost audit and execution evidence. Making or merging this package approves
no deletion and performs no live runtime observation.
