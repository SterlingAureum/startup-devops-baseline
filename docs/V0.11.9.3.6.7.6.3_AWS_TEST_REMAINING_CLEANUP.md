# v0.11.9.3.6.7.6.3 — remaining aws-test cleanup

## Actual progress and cause

The second runtime attempt stopped while waiting for `startup-apps` namespace
deletion after a user interrupt. Its Application/DNS/namespace deletion requests
had already run. Read-only investigation found one deleting ExternalSecret with
`externalsecrets.external-secrets.io/externalsecret-cleanup`; ESO was ready, but
the scoped namespace Role/RoleBinding were gone and its patch authorization was
denied. This supports a namespace teardown ordering defect: its controller
permissions disappeared before the dependent object's cleanup finished. The
interrupt stopped waiting; it did not create the lost permission.

A separately approved, UID/resourceVersion/policy/finalizer-tested single patch
removed only that ExternalSecret finalizer. Kubernetes completed namespace
deletion without removing its `kubernetes` finalizer. The repair completed at
September 13 12:33:49Z. No credential value, AWS mutation or Terraform command
was involved in that repair.

The 12:37:46Z observation found zero Applications, business namespaces, PVs,
NodeClaims, original Karpenter instances, captured business volumes, VPC ALBs,
Target Groups and test alias records. Four original On-Demand t3.medium system
nodes and their four gp3 30-GiB root disks remain. Two original NodePools and
three original NodeClasses remain with unchanged specs. This is not an
exhaustive volume inventory or a residual-cost audit.

Historical scripts, proofs, attempt markers and records are retained unchanged.
Do not rerun either consumed runtime attempt or the completed ESO repair.

## Candidate schedule and budget

This implementation proposes a two-hour extension of the previous overall
deadline: configuration cleanup stops at **2026-09-13T14:30:00Z**, latest start
14:10Z; overall cleanup completes by **16:00Z**. No prior approval is reused.
Fresh human schedule review and independent approval are required before
mutation. These deadlines do not stop controllers or billing automatically.

Existing reviewed allowances conservatively retain peak capacity until 16:00Z,
despite the observed compute reduction. Their whole-session projection is
**USD 35.20** against USD 36, leaving only USD 0.80 in the model. Historical
billing is unknown, no current quote was verified, and this is not a billing
guarantee. USD 20 remains a spend target, not the approved cap. If elapsed time
or observed resources no longer fit, stop for a new schedule/budget review.

## Merge and fresh verification

Apply to exact clean protected main `b111d58a70b6eae5c9380e1477a84b651e2555ab`.
Run the offline validator, commit and merge through protected checks. The live
entry must use the new post-merge HEAD == origin/main SHA and a clean main
checkout, with the supplied baseline as an ancestor. It never accepts a feature
branch or silently changes the Root's historical pinned deployment revision.

Private persistent inputs required:

- Existing recovery inputs JSON, isolated kubeconfig and recovery summary.
- Consumed `.7.6.2` workspace (runtime record, reviewed proof, attempt marker).
- Second runtime execution result stopped at `wait-namespace-startup-apps`.
- Successful one-time ESO repair `result.json`.
- Remaining-runtime observation directory with `summary.json`, `nodepools.stdout`
  and `nodeclasses.stdout`. The latter capture UIDs as well as specs.

Supply these existing paths through variables; do not upload raw records,
kubeconfig, state, controller outputs or resource IDs into the repository.
Unset old runtime/root/bootstrap/Terraform mutation confirmations. There must
be no `TF_VAR_*`, `TF_CLI_ARGS*`, `TF_WORKSPACE` or custom AWS endpoint overrides.

```bash
umask 077
NEW_SESSION="$(mktemp -d "$PRIVATE_ROOT/remaining-cleanup.XXXXXX")"

python3 scripts/execute-v0.11.9.3.6.7.6.3-aws-test-teardown.py verify-remaining \
  --inputs "$RECOVERY_INPUTS" \
  --workspace "$NEW_SESSION/workspace" \
  --output-directory "$NEW_SESSION/verify-output" \
  --prior-workspace "$CONSUMED_WORKSPACE" \
  --prior-result "$SECOND_RUNTIME_RESULT" \
  --repair-result "$ESO_REPAIR_RESULT" \
  --observation-directory "$REMAINING_OBSERVATION" \
  --expected-main "$EXPECTED_MAIN" \
  >"$NEW_SESSION/verify.json" 2>"$NEW_SESSION/verify.stderr"
VERIFY_EXIT=$?
printf 'verify_exit=%s\n' "$VERIFY_EXIT"
sha256sum "$NEW_SESSION/verify.json" "$NEW_SESSION/verify.stderr" \
  "$NEW_SESSION/workspace/runtime-proof.json" \
  "$NEW_SESSION/workspace/runtime-record.json"
cat "$NEW_SESSION/verify.json"
```

The script creates workspace/output directories exclusively; do not precreate
them. Verification reads STS/EKS/EC2/EBS/ELB/Route53 and Kubernetes object/status
metadata. It checks original state/inputs/kubeconfig/source pins, private failure
and repair hashes, absence of already-deleted resources, exact configuration
UIDs/specs, retained controller readiness, four original system nodes/ASG/root
disks, EKS VPC/CIDR and alias/ALB/Target Group absence. No Secret value is read.
Unexpected live instances, captured business disks, namespace/PV/PVC/claim
returns, or source/controller/configuration drift stop verification.

## Review and five-object deletion

Expected status is `aws-test-remaining-config-inputs-verified`. Its proof is valid
15 minutes from its original creation timestamp. Locally review the new private
record and candidate schedule. Create an exclusive reviewed copy changing only
`human_reviewed` to true and `schedule_review_required` to false, preserving the
timestamp, and obtain its SHA-256. Separately approve that exact proof, main,
state, record, candidate deadline and five-object scope. Merely generating or
merging this increment does not approve execution.

After approval, `execute-remaining` requires only
`CONFIRM_AWS_TEST_REMAINING_RUNTIME=cleanup-reviewed-aws-test-config-remnants-once`.
It accepts the same inputs/workspace, new output directory, reviewed proof and
its expected SHA. It creates `remaining-runtime-attempt.json` exclusively,
rechecks full scope, and deletes two NodePools and three NodeClasses once.
DELETE bodies contain UID/resourceVersion preconditions and normal background
propagation. No finalizer is forced, no deleted runtime resource is processed
again, no namespace/RBAC is recreated and no Terraform is called. Read-only
postchecks require zero configuration objects/claims and preserved system nodes.
Success produces `runtime-complete.json` for the independently approved EKS plan.

## Independent Terraform stages

Use this version's script for all later phases; old receipt/window hashes cannot
be substituted. `plan-eks` needs the new runtime-complete receipt; `plan-final`
needs the new EKS-complete receipt. Each plan and each saved apply require their
own existing phase confirmation and explicit approval. Providers use the
existing immutable lockfile; default workspace and Terraform 1.8–1.x are required.

EKS planning uses `-destroy -target=module.eks`; every planned managed delete or
recorded managed remote absence must stay inside `module.eks`. VPC/backup or
other-module deletion is rejected. Targeting can expand dependencies; if the
actual plan violates this gate, stop and review instead of bypassing it.
Apply uses the exact independently human-reviewed saved binary, with byte,
state, lockfile, workspace, source, plan-gate and 30-minute TTL checks. Captured
EKS-created security-group cleanup remains limited to verified ownership,
zero network interfaces and zero external references. System node/root disks
must disappear before the final plan. No Kubernetes API is required after EKS
deletion.

Final planning accounts for every remaining managed state address and reads
backup object/version metadata and credential-container version metadata. Its
proof also pins those private inventories. Any inventory change before saved
apply stops the operation. Final apply approval must expressly include permanent
deletion of all captured backup versions/delete markers and the credential
container's configured Terraform recovery window. No credential value is read.

Any failure or uncertainty preserves state, all markers and private evidence;
there is no automatic repair/retry or repeated saved apply. Legacy teardown
wrappers are forbidden. Full residual-cost audit, final bill reconciliation and
sanitized final execution evidence remain separate follow-up work.

## Durable correction for future rehearsals

For future generic runtime cleanup, let ExternalSecrets finish deletion while
their scoped controller Role/RoleBinding, SecretStore/ServiceAccount and ESO
remain available; verify their absence before deleting the namespace. Do not
turn this incident's one-time finalizer removal into an automatic fallback.
This increment avoids the completed namespace phase and records the defect;
it does not claim the historical generic namespace executor was rewritten.
