# Exact EKS dependency plan repair

Application baseline: `721419913123e18c37d3c3998256e89c6a81b8c1`.
Merge this offline increment and use the new clean protected-main SHA for every live phase.
This package grants no plan or deletion authorization.

## Why the previous plan stopped

The prior EKS-targeted plan produced binary, JSON and text artifacts, then failed the
module-only machine gate. Its failure was labeled `terraform-show-text` because the
runner stage had not been changed before invoking the gate. Terraform did not delete
resources; the original state is unchanged. The fix labels subsequent gate failures
`machine-plan-gate` or `saved-plan-gate` and retains the failed output and attempt marker.

The reviewed plan contains exactly 50 managed deletes, with no managed drift:

| Module | Managed deletes |
| --- | ---: |
| module.eks | 19 |
| module.cnpg_backup | 2 |
| module.external_secrets | 2 |
| module.github_actions_runtime_identity[0] | 1 |
| module.karpenter | 26 |

The 31 dependencies comprise CNPG/ESO IRSA roles and attachments, the runtime Access
Entry, and Karpenter controller permissions, node Access Entry, discovery tag and
interruption resources. Their exact definitions and instance counts are in the contract.
The gate derives each indexed address from the hash-bound private state, requires
exact set equality and compares the before ID with state. It rejects different resource
names of the same type, retained Karpenter node IAM/resource-discovery policy, missing
or extra changes, managed remote drift and create/update/replacement/read/no-op actions.
No VPC, bucket, credential container, DNS or ACM certificate deletion is accepted here.

## Original plan time cannot be renewed

`adopt-eks` is a read-only, once-only revalidation of the three exact saved artifacts.
It invokes version/workspace and saved-plan show; it never invokes init, plan or apply.
It pins original state and failure evidence, reproduces the JSON/text bytes and runs the
new gate plus fresh runtime/cloud scope checks. Existing directories are never reused.

Because the failed run did not write a plan proof, the adopted clock conservatively
starts at successful runtime completion, `2026-09-13T13:20:04Z`. Its 30-minute validity
ends at `2026-09-13T13:50:04Z`. Adoption time and modified file times cannot extend it.
Expiry stops at local inputs, before Terraform/AWS calls, and never triggers a replan.
An adopted proof remains `human_reviewed=false`; machine acceptance does not approve apply.

If that time has passed when this increment is merged, preserve the saved artifacts
and request separate authorization for **one fresh `plan-eks`**. That phase uses the
same exact gate, existing provider lock, read-only init, one EKS-targeted destroy plan
and show, in a new workspace/output. It starts a new proof clock for the newly produced
plan. The previously consumed plan approval cannot authorize it.

## Entry interface and evidence

```text
python3 scripts/execute-v0.11.9.3.6.7.6.4-aws-test-teardown.py PHASE
  --inputs PRIVATE_INPUTS_JSON
  --workspace NEW_PERSISTENT_WORKSPACE
  --output-directory NEW_PERSISTENT_OUTPUT
  --expected-main NEW_PROTECTED_MAIN_SHA
```

For `adopt-eks` and independently authorized `plan-eks`, also pass:

```text
--prior-workspace PRIOR_REMAINING_CLEANUP_WORKSPACE
--prior-result PRIOR_EKS_PLAN_FAILURE_JSON
--gate-observation PRIOR_PLAN_GATE_SUMMARY_JSON
```

`adopt-eks` additionally requires `--plan-directory PRIOR_EKS_PLAN_OUTPUT`.
The prior workspace must retain the successful runtime receipt, bound record and
consumed EKS-plan marker. The new workspace receives an unchanged runtime record and
a current-main/current-contract continuation receipt. This receipt does not authorize
Terraform apply. State, account, restricted endpoint, original four system nodes and
four root disks, absent Applications/business namespaces/PVs/NodeClaims/configurations,
ALB/target groups and exact test alias are checked before EKS plan/apply.

For `apply-eks`, `plan-final` and `apply-final`, reuse this new workspace and supply
`--proof PRIVATE_REVIEWED_PROOF --expected-proof-sha256 APPROVED_PROOF_DIGEST`.
Each output directory is new. Each phase has its own exclusive attempt marker.

| Phase | Separate authorization and boundary |
| --- | --- |
| adopt-eks | Read-only show/gate and scope checks; no init or plan |
| plan-eks | New explicit plan-only approval; existing lockfile, one plan and show |
| apply-eks | Human review of exact text plan and proof; approval of all exact 50 deletes and the captured owned/unreferenced EKS-created security group if retained |
| plan-final | Separate plan-only approval bound to this version's EKS completion receipt; account for all remaining managed state, capture backup versions and container metadata |
| apply-final | Independent exact saved-plan approval including permanent bucket version deletion and credential-container Terraform recovery semantics; recheck inventory digests |

Live credential values are never read during teardown. Preserve all historical evidence,
provider lock, isolated kubeconfig, state and one-time markers. No completed runtime
cleanup, bootstrap, state editing, finalizer repair, automatic retry or automatic fallback
is added. Apply verifies current main, inputs, runtime record, state, saved bytes,
Terraform version, workspace, lock and proof TTL before its one saved-plan apply.

## Schedule and budget

The cleanup deadline remains `2026-09-13T16:00:00Z`; every Terraform phase must start
with at least 900 seconds remaining. The total limit remains 36 USD. The existing
reviewed allowance model projects 35.20 USD; historical billed spend is unknown and
this is not a current price quote or billing guarantee. No automatic deadline or budget
extension is included. A fresh schedule/budget review is required if time is insufficient.

## Offline validation

```bash
bash scripts/validate-v0.11.9.3.6.7.6.4-eks-dependency-plan-repair.sh
```

The new suite verifies exact address/ID/definition boundaries, rejected substitutions
and drift, original clock preservation, expired-adoption early stop, byte-preserving
adoption with no plan/apply, precise gate failure labels, separate flags, and the final
inventory guard. It invokes the frozen preceding regression suites. Test fixtures contain
no live account, state, credentials or resource identifiers. Aggregate CI is still required.
