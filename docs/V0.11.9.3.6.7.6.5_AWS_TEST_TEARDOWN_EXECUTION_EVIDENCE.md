# v0.11.9.3.6.7.6.5 — aws-test teardown execution evidence

Implementation baseline: `00de317b356d9318288fbd30acaf8df0caa6ca93`.
This is an offline evidence increment. It records user-confirmed redacted outputs
and private file fingerprints; it does not inspect private raw files or observe AWS.

## Recorded deletion chain

| Stage | Result | Scope |
| --- | --- | --- |
| Remaining configuration | Completed September 13 13:20:04Z | Two NodePools and three NodeClasses; completed runtime deletions were not repeated |
| EKS dependency saved plan | Produced 14:07:36Z | Exactly 50 managed deletes: EKS 19 and explicitly selected dependent resources 31 |
| EKS saved apply | Terraform succeeded; executor stopped 14:27:02Z | All 50 deleted addresses absent from state; 40 retained managed addresses intact. One available ENI prevented the orphan security-group postcheck from passing |
| Independent ENI/SG cleanup | Completed 14:51:12Z | One captured unattached ENI and its captured security group; separate approval, preserved state, new one-time marker |
| Final saved plan | Produced 14:55:16Z | Remaining 40 managed deletes; nine captured backup versions, zero delete markers; container metadata fixed in private inventory |
| Final saved apply | Completed 15:05:58Z, exit 0 | Saved plan applied once; managed state empty and executor cloud absence checks passed |
| September 14 local check | Confirmed 00:59:45Z | Same main, empty state hash and four critical private file hashes; no fresh cloud observation |

The earlier stopped EKS wrapper is retained as a failure record. Its exit 1 was a
post-apply safety stop and is not rewritten as a successful wrapper exit. The
separately approved ENI/SG helper completed the remaining postconditions and wrote
the `.6.4` EKS completion receipt. None of those consumed attempts can be rerun.

## Evidence boundary

The public contract contains release-independent stage facts and private hashes,
never account IDs, cloud resource IDs, credentials, endpoints or persistent private
paths. Private inventory, raw output, state backups and attempt markers remain
outside the repository. A hash records the supplied file identity; static validation
does not authenticate raw cloud data that was not supplied to the package producer.

The final state was mode `0600`, 4536 bytes, with zero resource blocks, instances
and managed instances. EKS, VPC, captured volumes, test DNS, ALB and backup bucket
absence checks passed. The credential container passed the accepted absent-or-
deleting-tombstone condition; the actual branch and recovery period are not claimed.
The volume check covers captured volumes and is not an exhaustive account inventory.

## Historical budget and windows

The historical model estimated USD 35.20 against a USD 36 limit. USD 20 was a target,
not a cap. Historical billed spend is unknown; the estimate is neither a current
price quote nor a billing guarantee. Completion preceded September 13 16:00Z.
Historical approvals and proof TTLs are consumed and unchanged. This package grants
no new window, budget spending authorization, plan, deletion or audit approval.

## Next checkpoint

A complete residual-cost audit has not run. An empty Terraform state and passed
postconditions do not establish that every untracked chargeable resource is absent.
The next increment must implement a guarded aws-test read-only residual-cost audit:
new exact protected-main, exact private account, readable empty state, persisted
private evidence, strict API error classification and separately reviewed preflight,
executor verify and one-time audit approval. It must not repeat teardown, require a
live Kubernetes API for the deleted cluster, or reuse yesterday's mutation window.

## Offline validation and integration

```bash
bash scripts/validate-v0.11.9.3.6.7.6.5-teardown-execution-evidence.sh
```

The validator binds contract bytes and predecessor source hashes, checks state/count,
clock, receipt and privacy invariants, and runs the existing `.6.4` offline chain.
It does not call AWS, Terraform or Kubernetes. CODEOWNERS and aggregate quality gates
include this checkpoint. GitHub CI must pass before merging; local aggregate execution
may stop at the existing Docker prerequisite.
