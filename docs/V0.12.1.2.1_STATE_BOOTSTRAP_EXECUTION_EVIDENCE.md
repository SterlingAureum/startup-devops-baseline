# v0.12.1.2.1 State-Bootstrap Execution Evidence

v0.12.1.2.1 records the redacted terminal evidence for the first reviewed
state-bootstrap creation and its read-only post-apply recovery. The foundation
was created by the exact reviewed saved plan, the original apply was not
repeated, and recovery completed live S3, KMS and IAM-policy validation at
`2026-09-25T08:21:14Z`.

The canonical evidence contract is
`delivery/contracts/v0.12.1.2.1-state-bootstrap-execution-evidence.json`.

## Bound execution chain

The evidence binds:

- incident protected main `bcd653ca3705df177864c5a3f12201f9a987191f`;
- recovery protected main `10c018fa935f777d6bd04c43fa856e5e650f53b5`;
- the private plan, apply and recovery request SHA-256 digests;
- the reviewed binary plan, text/JSON views and plan-gate digests;
- the byte-identical applied local-state digest; and
- the final recovery-result and live-validation digests.

The reviewed plan contained 13 managed creates and six data-source changes. It
contained no update, delete, replacement, import, unexpected resource or IAM
attachment. Human review passed before the separately approved apply.

## Incident and recovery outcome

Terraform apply succeeded, then post-apply validation stopped because a KMS
alias was passed to an API that requires a key ID or ARN. The observed error
was `InvalidArnException`. v0.12.1.2.0.1 repaired that read and resumed only
read-only validation; it did not run Terraform init, plan or apply.

The terminal recovery verified:

- all 13 expected managed state addresses and matching preserved state copies;
- S3 versioning, bucket-owner enforcement, public-access blocking, SSE-KMS,
  bucket keys, TLS-only access and a non-public bucket policy;
- an empty state bucket with zero object versions before migration;
- an enabled customer-managed KMS key with rotation enabled; and
- five root-scoped IAM policies with zero attachments.

The bootstrap state remains a private local artifact. No remote backend was
initialized and no state was migrated.

## Privacy and authority boundary

This public checkpoint contains only commits, SHA-256 digests, bounded counts,
boolean control results, terminal status and completion time. It contains no
AWS account ID, bucket identity, resource ARN, state bytes, plan bytes, private
filesystem path or raw AWS output.

Applying this repository increment runs no AWS or Terraform operation. It does
not authorize retry, destroy, IAM attachment or state migration. Preserve the
private plan, apply, recovery and state evidence unchanged under owner-only
permissions.

v0.12.2 remains a separate change and approval boundary. It must design and
verify the guarded non-empty local-to-remote migration, immutable local backup,
lineage/address preservation, lock contention, zero-change plan and controlled
object-version recovery before any backend migration is attempted.

## Offline validation

```bash
bash scripts/validate-v0.12.1.2.1-state-bootstrap-execution-evidence.sh
```

The validator performs deterministic contract, predecessor, documentation,
privacy and fail-closed mutation checks. It invokes no AWS or Terraform
operation.
