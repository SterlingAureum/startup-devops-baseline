# v0.12.2.3.1.0.2.0.1.2 Dual-Form Pre-Apply State Gate

The second stopped reconciliation observed the original reviewed canonical
state byte-for-byte. No apply artifact exists. Together, the two attempts prove
that `terraform state pull` can return either of two exact representations
whose only difference is `check_results`.

This repair replaces the single-observation byte gate with a finite two-item
allowlist: the reviewed canonical state and the reviewed `check_results`-
normalized state. Both forms have identical lineage, serial, resources,
outputs, address inventory and six-key semantic projection. Any third digest
or any semantic difference remains fail closed.

Verification reconstructs both stopped attempts and runs no operational
command. Execution retains the exact saved refresh-only plan, state/S3 history
checks and post-apply gates. It does not authorize init, a new plan, unsaved
apply, state push, remote resource mutation, retry or rollback.

After merge, create a fresh request from the `.0.1.2` example, use a new output
directory and approval window, and run command-free verify before requesting a
separate execution approval.
