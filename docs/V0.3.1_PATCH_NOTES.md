# v0.3.1 Patch Notes

This patch is generated from the current repository structure, not from a generic template.

## Files included

```text
apps/demo-api/helm/values.yaml
apps/demo-api/helm/templates/service.yaml
apps/demo-api/helm/templates/ingress.yaml
apps/demo-api/helm/templates/rollout.yaml
clusters/local/platform/demo-api.yaml
scripts/validate.sh
docs/V0.3.1_IMPLEMENTATION.md
docs/V0.3.1_PATCH_NOTES.md
```

## Main changes

- Adds `demo-api-stable` and `demo-api-canary` Services.
- Updates stable Ingress backend to `demo-api-stable`.
- Adds `trafficRouting.nginx.stableIngress` to the Rollout.
- Adds Argo CD `ignoreDifferences` for Rollout-managed Service selectors.
- Updates validation to check ingress canary routing resources.

