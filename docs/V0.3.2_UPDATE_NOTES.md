# v0.3.2 Update Notes

## Version

```text
v0.3.2-ghcr-image-publishing
```

## Goal

Add a registry-based image publishing workflow while preserving the existing local kind workflow.

## Added files

```text
.github/workflows/validate.yaml
.github/workflows/demo-api-image-publish.yaml
apps/demo-api/helm/values-ghcr.yaml
scripts/set-demo-api-image.sh
scripts/set-demo-api-local-image.sh
scripts/check-ghcr-demo-api-image.sh
docs/GHCR_IMAGE_WORKFLOW.md
docs/V0.3.2_UPDATE_NOTES.md
```

## Why values.yaml is not changed by default

The current repository still supports local kind development. The default `values.yaml` can remain local-friendly:

```yaml
image:
  repository: startup-devops-baseline/demo-api
  pullPolicy: Never
```

Use `scripts/set-demo-api-image.sh` to explicitly switch to a GHCR image tag when you want to test the registry-based release flow.

After GitHub Actions publishes an image, deploy it with:

```bash
SHORT_COMMIT=$(git rev-parse --short HEAD)
IMAGE_TAG="sha-${SHORT_COMMIT}"
IMAGE_TAG=${IMAGE_TAG} ./scripts/set-demo-api-image.sh
git add apps/demo-api/helm/values.yaml
git commit -m "release: update demo-api image to ${IMAGE_TAG}"
git push
```

Then watch rollout:

```bash
./scripts/rollout-watch.sh
```

## Suggested tag

After validation:

```bash
git tag v0.3.2
git push origin v0.3.2
```
