apiVersion: v1
kind: Namespace
metadata:
  name: karpenter-fis-smoke
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: karpenter-fis-spot-test
  namespace: karpenter-fis-smoke
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: karpenter-fis-spot-test
  template:
    metadata:
      labels:
        app.kubernetes.io/name: karpenter-fis-spot-test
    spec:
      nodeSelector:
        karpenter.sh/nodepool: application-spot-fis
        workload: application
      tolerations:
        - key: dedicated
          operator: Equal
          value: application-spot-fis
          effect: NoSchedule
      containers:
        - name: nginx
          image: public.ecr.aws/docker/library/nginx:1.27-alpine
          imagePullPolicy: IfNotPresent
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 2
            periodSeconds: 5
          resources:
            requests:
              cpu: 750m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
      terminationGracePeriodSeconds: 30
