# EKS module

This module is intentionally a no-resource skeleton in v0.4.0.

A later phase will add the EKS control plane, managed node group, IAM roles,
OIDC integration, access configuration, and managed add-ons.
